node_modules
.next
out
.git
.canopy-dev
*.tgz
cdk.out
# The CDK app from `init-deploy aws`. The image never runs it, and inside the
# build context `next build` would type-check it, failing wherever aws-cdk-lib
# is not installed.
infrastructure

# Secrets must never enter the image build context.
.env*

# Do NOT ignore vendor/ -- file: dependencies (vendored tarballs) must reach
# the Docker build context, or the install step in Dockerfile.cms will fail to
# resolve them.
