# Fixture for cms-deploy.test.ts's image-platform tests. `cdk synth` stages and
# fingerprints this directory as a Docker image asset but never builds it, so
# the contents only need to be a valid build context.
FROM scratch
