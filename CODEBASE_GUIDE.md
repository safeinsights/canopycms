<\!-- This file is maintained by the update-codebase-guide agent (.claude/agents/update-codebase-guide.md). -->
<\!-- Do not edit manually — changes will be overwritten. -->

## Package Structure

| Package              | Location                       | Purpose                                                                         |
| -------------------- | ------------------------------ | ------------------------------------------------------------------------------- |
| canopycms            | packages/canopycms/            | Core CMS library                                                                |
| canopycms-next       | packages/canopycms-next/       | Next.js adapter (config wrapper, catch-all handler, context, client components) |
| canopycms-auth-clerk | packages/canopycms-auth-clerk/ | Clerk auth plugin                                                               |
| canopycms-auth-dev   | packages/canopycms-auth-dev/   | Dev auth plugin with cache-writer, JWT verifier for dev mode                    |
| canopycms-cdk        | packages/canopycms-cdk/        | AWS CDK constructs for deployment (VPC, EFS, Lambda, EC2 worker)                |

**Apps** (in apps/, not packages/):

| App      | Location       | Purpose                                           |
| -------- | -------------- | ------------------------------------------------- |
| example1 | apps/example1/ | Example Next.js app showing CanopyCMS integration |
| test-app | apps/test-app/ | E2E test application                              |

## Source Code Organization

The codebase uses a modular structure with clear separation:

**Core Modules** (packages/canopycms/src/):

| Module          | Location            | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| authorization/  | src/authorization/  | Unified access control (branch + path permissions, groups, protected-base-branch policy)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| config/         | src/config/         | Configuration types, Zod schemas, validation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| schema/         | src/schema/         | Schema loading, resolution, and CRUD (SchemaOps) from .collection.json                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| paths/          | src/paths/          | Path utilities with branded types (LogicalPath, PhysicalPath); `sanitizeBranchName` lives in dependency-free `branch-name.ts`, not `branch.ts` (node:fs) or the barrel — see [Path Utilities Module](#path-utilities-module)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| operating-mode/ | src/operating-mode/ | Operating mode strategies (prod, dev)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| api/            | src/api/            | API handlers, middleware, route builder                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| http/           | src/http/           | HTTP request handling (router, types)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| editor/         | src/editor/         | React editor components, contexts, hooks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| validation/     | src/validation/     | Validation utilities (field traversal, references); field-traversal.ts handles `type: 'group'` by recursing into children transparently (no data-key consumed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| static/         | src/static/         | Framework-agnostic static-generation helpers: `collectStaticPaths(buildCtx, opts)` → neutral `StaticPathEntry[]` (urlPath, collapsed segments, slug, entryType) for adapter-specific static generation; validates entries via `assertBuildEntriesValid` when `isBuildMode()` (fails the production build on schema-invalid entries — e.g. abandoned create-scaffolds — rather than silently dropping the route); types `StaticPathEntry`/`CollectStaticPathsOptions`; `findInvalidEntries(items)`/`assertBuildEntriesValid(items, phaseLabel)` — reusable schema-validity guard over `ListEntriesItem[]` (also used unconditionally by `build/generate-ai-content.ts`), `InvalidBuildEntry` type (sitemap + SEO helpers deferred — see .claude/future-tasks/static-export-sitemap.md and static-export-seo-metadata.md) |
| utils/          | src/utils/          | Shared utilities (error handling, debug logging, title-field: resolveEntryTitle, findInvalidTitleFields, findTitleFieldsInLists — all traverse `type: 'group'` fields transparently by recursing into their children; body-field: countBodyFields, findInvalidBodyFields; flatten-group-fields: flattenGroupFields — recursively flattens inline groups for data-layer iteration; sanitize-href: sanitizeHref)                                                                                                                                                                                                                                                                                                                                                                                                          |
| auth/           | src/auth/           | Authentication plugin interface and cache system                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| assets/         | src/assets/         | Asset store v2: pluggable storage adapters (S3/local), upload finalize pipeline (sniff/hash/dims/SVG-sanitize), on-demand image-transform engine (`/assets/t/{directives}/{hash32}/{slug}.{ext}`) — see [Assets Module](#assets-module) below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| worker/         | src/worker/         | CMS Worker daemon for background tasks (git sync, task processing, auth cache refresh)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| task-queue/     | src/task-queue/     | Generic file-based persistent task queue (zero Canopy dependencies; EFS/NFS-safe)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| cli/            | src/cli/            | CLI bootstrapping (`pnpm exec canopycms init`, `init-deploy aws`, `worker run-once`, `sync`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| test-utils/     | src/test-utils/     | Shared test utilities (mock factories, console spy, git repo init); exported as `canopycms/test-utils`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

**Top-level files** (intentionally not modularized):

| File                        | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| services.ts                 | CanopyServices factory with git operations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| config-test.ts              | Test-only config helpers: `defineCanopyTestConfig()` defaults `mode: 'dev'` (the real schema requires `mode` with no default — SEC-C1) so existing test configs stay terse; `createTestServices()` builds `CanopyServices` from an inline schema, bypassing `.collection.json`/BranchSchemaCache. Not for production code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| context.ts                  | Context creation and management; `getContext()` awaits `services.refreshActiveBranch()` first (dev-mode reads follow git HEAD per request); exports `CanopyBuildContext` (build-safe: buildContentTree, listEntries, read, readByUrlPath, services) and `CanopyContext extends CanopyBuildContext` (adds user); schema resolution memoized per call; `read`/`readByUrlPath` return `{ data, path, meta: { physicalPath } }` (readByUrlPath: `…                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | null`) where `meta.physicalPath`is the ABSOLUTE filesystem path to the resolved entry file — server-only (reveals home dir / EFS mount / branch name; do NOT serialize to client or embed in public output), intended for build/server-side reads of colocated artifacts (e.g. a sibling`profile.json`via`path.join(path.dirname(result.meta.physicalPath), 'profile.json')`); readByUrlPath uses ContentStoreError.code for probing instead of regex message matching, and is null-safe for invalid/junk paths (skips candidates whose slug fails validation, e.g. `/favicon.ico`); also null-safe for FORBIDDEN reads — a path-permission denial resolves to `null`(debug-logged) instead of throwing, so server-component pages using`if (!result) return notFound()`render a 404 instead of an unhandled 500; the strict`read()` API is unaffected and still throws on FORBIDDEN |
| types.ts                    | Core types (BranchContext, BranchContextWithSchema, BranchMetadata — incl. `rebaseFailure?: { message, firstAt, lastAt }` set by the worker on rebase failure, cleared on next success/submit, `syncFailureReason?: string` — short reason the last GitHub sync task failed, set alongside `syncStatus: 'sync-failed'` (worker/cms-worker.ts), cleared on next successful sync, SyncStatus, ConflictStatus, PullRequestState — `'open' \| 'closed' \| 'merged'`); `WorkerStatusReport` — wire shape of `worker-status.json` written by the worker daemon (worker/worker-status.ts), read by `GET /admin/status`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| branch-metadata.ts          | Branch metadata persistence; layered concurrency (in-process `withLock` + server-enforced `withOccFileLock` + OCC via `utils/occ-json-write.ts`) — see docs/concurrency.md; `buildMergedBranchUpdate(branchName, now?)` — shared archived-branch metadata delta (status 'archived', pullRequestState 'merged', mergedAt) used by both CmsWorker's merge-poll (`pollMergeState`) and the manual `markAsMerged` API (api/branch-merge.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| branch-registry.ts          | Branch tracking and listing; generation-token snapshot cache (resource-generation.ts) — no more `branches.stale.json`; eager regen on `invalidate()`, `get()`-miss backstop                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| branch-health.ts            | Admin-facing health scan of every directory under a branches root, including ones `branch-registry.ts` quarantines out of normal listings. `scanBranchHealth(baseRoot, { baseBranchName })` mirrors the registry's own directory-scan skip rules (skips non-directories and dot-prefixed names) and classifies each dir as `healthy` (valid branch.json), `corrupt-metadata` (branch.json present but unparseable/unreadable), or `orphan` (no branch.json — e.g. a partial-delete crash). Types `BranchHealthEntry`/`BranchHealthKind`; exported `provisioningLockPath(baseRoot, dirName)`. For orphan/corrupt entries, also stats the branch-workspace provisioning-lock's freshness (`provisioningLock: { mtime, ageMs }` at `{baseRoot}/.{dirName}.init.lock` — same path `branch-workspace.ts`'s `ensureGitWorkspace()` uses). Never throws on a single bad directory; tolerates a missing `baseRoot` (returns `[]`)                                                                                                                                                                                                                                                                                                                                                        |
| branch-workspace.ts         | Branch workspace management                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| branch-schema-cache.ts      | Per-branch schema caching (always file-based; no in-memory path); generation-token cache (resource-generation.ts) — no more `.stale` file; exports `SCHEMA_GENERATION_RESOURCE`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| settings-workspace.ts       | Settings branch workspace; `SettingsWorkspaceManager.ensureGitWorkspace` has a rename guard — before `GitManager.initializeWorkspace` (which would otherwise run `git checkout --orphan` + `rm -rf .` on an existing workspace), it checks `GitManager.repoExistsAt(settingsRoot)` and, if a workspace already exists but is checked out on a DIFFERENT branch than the newly-resolved settings branch name, throws loudly instead of silently wiping `permissions.json`/`groups.json` — the failure mode when `deploymentName`/`CANOPYCMS_DEPLOYMENT_NAME` changes on a deployment with an already-populated settings workspace (see operating-mode/deployment-name.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| settings-branch-utils.ts    | Settings branch utility helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| content-store.ts            | Content persistence (getCollectionEntryPaths for index-based minimal metadata); ContentStoreError with typed codes (ContentStoreErrorCode: NOT_FOUND, NO_SCHEMA_ITEM, FORBIDDEN, VALIDATION) for structural error matching; all internal assertion helpers (e.g. assertCollection) propagate error codes; `resolveReferencesInData` recurses through inline groups; third constructor arg `ContentStoreOptions` (`{ indexFreshnessIntervalMs }`, default 1s; tests pass 0); `idIndex()` probes the on-disk generation marker (throttled) to detect mutations by OTHER processes; `readById` self-heals stale lookups (suspicious result → one time-boxed forced rebuild + retry); `write()` cross-process existence guard consults the actual directory listing before recreating a missing expected file and can throw `ContentConflictError` (also thrown on expectedVersion/mtime mismatch); mutator (`write`/`delete`/`rename`) lock keys are namespaced content-ID keys (`id:`/`create:`) derived from an unlocked `buildPaths()` pre-pass (directory-scan for the slug); `buildPaths()` is re-run a second time INSIDE the lock (`withLock`/`withLocks`, `utils/async-mutex.ts`) so the pre-pass's classification can't race a concurrent writer — see docs/concurrency.md |
| content-reader.ts           | Content reading; resolves `entry:ID` links in body at read time (opt-out via `resolveEntryLinks: false`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| entry-link-resolver.ts      | Entry link resolution: `resolveEntryUrl()`, `resolveEntryLinksInText()`, `extractEntryLinkIds()`; resolves `entry:CONTENT_ID` patterns in markdown body to URL paths; skips fenced/inline code blocks; supports custom resolver via `EntryLinkUrlResolver` callback; missing IDs become `#` dead links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| content-listing.ts          | Shared content-listing utilities (entry parsing, ordering, filesystem reading, batch listing via listEntries); `ListEntriesItem` includes `urlPath` with index entries collapsed (e.g., `/guides` not `/guides/index`; root index returns `/`) and optional `schema` (the entry type's field definitions, when resolvable from the collection)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| content-tree.ts             | Content tree builder for adopters (navigation, sitemaps, breadcrumbs); `defaultBuildPath` collapses index entries to parent collection URL path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| content-id-index.ts         | Content ID indexing; getEntriesInCollectionTree (tree traversal including subcollections), getAllEntryLocations (all entries across collections for entryType-only queries)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| content-index-registry.ts   | In-process registry connecting branch-mutating operations (git checkout/merge/rebase, content sync) to ContentStore instances whose ContentId indexes they make stale; stores self-register on construction (`registerContentIndexForInvalidation`, WeakRef-held), mutations call `invalidateContentIndexesForRoot` (prefix-matched, lazy rebuild). In-process only — cross-process handled by content-index-generation.ts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| content-index-generation.ts | Now a thin ContentId-index-specific wrapper over the generic marker primitive in `resource-generation.ts`: `bumpContentIndexGeneration`/`readContentIndexGeneration` (marker at `{root}/.canopy-meta/content-index.generation`); `invalidateContentIndexesDurable(root)` — entry point for mutations that only ever touch content files (marker bump + in-process registry invalidation); `invalidateBranchContentCaches(root)` — entry point for BULK working-tree mutations (git checkout/merge/rebase, content sync, CLI sync, migrate) that can also touch `.collection.json`; bumps both the `content-index` marker and the schema-cache marker (`SCHEMA_GENERATION_RESOURCE`, branch-schema-cache.ts), then invalidates the in-process registry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| resource-generation.ts      | Generalized on-disk generation-marker primitive underlying all durable cross-process cache invalidation: per-resource marker file at `{root}/.canopy-meta/{resource}.generation` holding a random token (not a counter — avoids read-modify-write lost updates); `bumpResourceGeneration(root, resource, { mustSucceed? })` (default "hint" flavor logs-and-swallows failures; `mustSucceed: true` for callers where a lost bump means unbounded staleness, e.g. a registry's `invalidate()`), `readResourceGeneration`, `isGenerationCurrent(snapshotToken, read)`; `content-index-generation.ts`, `branch-registry.ts`, and `branch-schema-cache.ts` are all instances of this protocol — see docs/concurrency.md for the residual-staleness-window analysis (A/B/C/E)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| entry-schema.ts             | Entry schema definitions (defineEntrySchema, TypeFromEntrySchema); defineBlockTemplate() defines a reusable `block` template once for reuse across schemas (const-inference identity helper like defineEntrySchema; correct discriminated-union type via TypeFromEntrySchema); DistributeBlockTemplate produces proper discriminated unions for block fields; InferableField includes resolvedSchema (typed reference resolution), entryTypes, and collections for reference field scoping; defineInlineFieldGroup() creates InlineGroupFieldConfig (visual-only grouping, flat data); defineNestedFieldGroup() is a thin wrapper over ObjectFieldConfig (creates a named data key); FlattenInlineGroups<Fields> is an internal type helper that recursively unwraps inline groups for TypeScript type inference                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| sync-core.ts                | Prompt-free core of working-tree↔branch-clone content sync (extracted from cli/sync.ts, which now imports it). Exports `assertWithinDir`, `safeReplaceDir`, `listFilesRecursive`, `copyDir`, `diffContentTrees` (byte comparison, not mtime; also used read-only by dev-content-watcher.ts), `isContentTreeDiffEmpty`, `pushContentToWorkspace` (copy + commit + optional sync-base tag; now used only by the CLI `sync push`), `SYNC_BASE_TAG` (`'canopycms-sync-base'`); types `ContentTreeDiff`/`PushContentToWorkspaceOptions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| dev-content-watcher.ts      | `startDevContentWatcher(services, options)`: dev-mode chokidar watcher that detects divergence between working-tree `content/**` and the served branch clone (warn-only). Per `dev.contentSync`: 'warn' (default) logs which files diverged, 'off' is a no-op. Re-resolves the served active branch on every check (follows git-HEAD switches) and dedupes watchers across HMR via a module-level registry keyed by the working-tree content dir. Never writes (no `pushContentToWorkspace`). No-op outside dev mode or when the working-tree content dir is absent; returns a disposer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| entry-schema-registry.ts    | Entry schema registry for reusable field definitions; validates isTitle (string only) and isBody (at most one, markdown/mdx only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| git-manager.ts              | Git operations wrapper; `GitManagerOptions.skipIndexMarker` skips the on-disk index-generation marker after working-tree mutations (set for settings workspaces; in-process invalidation still runs); exported standalone `ensureGitExcludePattern(repoPath, pattern)` (idempotent `.git/info/exclude` entry; extracted from the instance method for workspace-creating code that holds no GitManager, e.g. CLI sync auto-create); exported static `GitManager.repoExistsAt(workspacePath): Promise<boolean>` — the `rev-parse --git-dir` + `GIT_CEILING_DIRECTORIES` existence check factored out of `initializeWorkspace`'s inline check, reused by both `initializeWorkspace` (clone-vs-reuse decision) and settings-workspace.ts's rename guard                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| github-service.ts           | GitHub API integration; `createOrUpdatePullRequest()` — shared idempotent create-or-update-PR helper (`markReadyIfDraft`, `AbortSignal` support) used by both `GitHubService.createOrUpdatePR` and CmsWorker's `push-and-create-or-update-pr` task; `createCanopyOctokit()` — Octokit factory with `@octokit/plugin-throttling` attached; `shouldRetryRateLimit`/`shouldRetrySecondaryRateLimit` — retry predicates for primary/secondary GitHub rate limits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| comment-store.ts            | Comment persistence; layered concurrency (in-process `withLock` + server-enforced `withOccFileLock` + OCC via `utils/occ-json-write.ts`) — see docs/concurrency.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| url-path-resolver.ts        | URL path to content resolution (resolveUrlPathCandidates: decomposes URL path into ordered entryPath/slug candidates; root path `/` returns `{ entryPath: contentRoot, slug: 'index' }`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| reference-resolver.ts       | Reference resolution; loadReferenceOptions supports optional collections (with subcollection tree traversal) and/or entryTypes filtering; at least one scope required                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| build-mode.ts               | Static deploy detection (`isDeployedStatic`), build-phase safety net (`isBuildMode`), `STATIC_DEPLOY_USER`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| user.ts                     | User utilities                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| server.ts                   | Server entrypoint exports (includes `resolveEntryUrl`, `resolveEntryLinksInText`, `extractEntryLinkIds`, `EntryLinkUrlResolver`; static helpers `collectStaticPaths` + types `StaticPathEntry`/`CollectStaticPathsOptions`; `startDevContentWatcher` + type `StartDevContentWatcherOptions`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## API Layer

**Location**: packages/canopycms/src/api/

| Endpoint                          | Handler               | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| /api/canopycms/branches           | branch.ts             | Create/list branches; `BranchListResponse` includes optional `defaultBranch` — the server's effective default branch (detected active branch in dev), read per-request in `listBranchesHandler`; each `BranchListItem` (BranchMetadata + optional `isProtected`/`readOnly`) is server-computed per-request via `getBranchProtection` (authorization/protected-branch.ts); `deleteBranchHandler` rejects (400) deleting the protected base branch before any permission check |
| /api/canopycms/branch-status      | branch-status.ts      | Get status, submit PR; submit guarded by `submittableBranch` (403 on the protected base branch, both modes) then `canPerformWorkflowAction(..., { isProtectedBranch })`; on `ctx.services.submitBranch` failure, `isNonFastForwardRejection` (utils/git.ts) classifies the error — a non-fast-forward push rejection returns 409 with an actionable rename/reconcile message, everything else keeps the existing 500 path                                                    |
| /api/canopycms/branch-withdraw    | branch-withdraw.ts    | Withdraw PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| /api/canopycms/branch-review      | branch-review.ts      | Request changes                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| /api/canopycms/branch-merge       | branch-merge.ts       | Merge & cleanup                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| /api/canopycms/content            | content.ts            | Read/write content; validates entry links on write, returns `entryLinkWarnings`; runs adopter `validateEntry` hook before `store.write` (error issues reject with 422; warning issues returned as `validationWarnings` on `ContentWriteResponse`)                                                                                                                                                                                                                            |
| /api/canopycms/entries            | entries.ts            | Entry management; list is cursor-paginated (offset cursor; default limit 50, max 200); GET query strings coerced (`limit` via z.coerce.number, `recursive` via queryBooleanSchema)                                                                                                                                                                                                                                                                                           |
| /api/canopycms/assets             | assets.ts             | Asset presign/finalize/upload/list/delete; separately-registered `assetRawRoute` (`GET /assets/raw/{key...}`, not part of `ASSET_ROUTES`/client codegen — returns raw bytes) serves public objects and, on a cache miss under the `assets/t/` transform prefix, lazily runs `applyTransform` via internal helper `serveLazyTransform` and caches the result under its canonical key (dev-mode emulation of the future prod transform Lambda)                                 |
| /api/canopycms/comments           | comments.ts           | Comment CRUD                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| /api/canopycms/groups             | groups.ts             | Group management; GET /groups/internal response includes `version` (OCC version off groups.json); update handler maps `SettingsVersionConflictError`/`SettingsFileConflictError` (authorization/settings-file-store.ts) to 409                                                                                                                                                                                                                                               |
| /api/canopycms/permissions        | permissions.ts        | Permission management; GET response includes `version` (OCC version off permissions.json); update handler maps `SettingsVersionConflictError`/`SettingsFileConflictError` (authorization/settings-file-store.ts) to 409                                                                                                                                                                                                                                                      |
| /api/canopycms/reference-options  | reference-options.ts  | Reference field options; query params: collections, entryTypes, displayField, search (at least one of collections/entryTypes required)                                                                                                                                                                                                                                                                                                                                       |
| /api/canopycms/resolve-references | resolve-references.ts | Resolve reference IDs to data                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| /api/canopycms/user               | user.ts               | Current user info                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| /api/canopycms/schema             | schema.ts             | Schema CRUD (collections, entry types, ordering) - admin only; GET /:branch/schema sends flat `WireFlatSchemaItem[]` — collection items carry NO nested `collections` subtree on the wire (children linked via `parentPath`; embedded `entries`/`order` kept); in-memory `FlatSchemaItem.collections` unchanged; single-collection GET (getCollection) still returns a nested subtree; all mutation handlers map `SchemaStoreBusyError` (schema/schema-store.ts) to 409      |

### Admin Endpoints

**Location**: packages/canopycms/src/api/admin.ts, api/admin-branch-health.ts

Admin-only (`guards: ['admin']`), namespace `admin`. `admin-branch-health.ts`'s routes are split into their own file to keep `admin.ts` under its size budget, then merged into the single `ADMIN_ROUTES` export from `admin.ts` — router (http/router.ts) and generate-client.ts both still import one `ADMIN_ROUTES`.

| Endpoint                                         | Route name      | Handler                | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------ | --------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET /admin/status                                | status          | admin.ts               | Task queue stats + worker lock liveness (alive/stale/absent, from `.worker-lock` mtime vs. threshold) + last `worker-status.json` snapshot                                                                                                                                                                                                                                                                                                     |
| GET /admin/tasks/:status                         | listTasks       | admin.ts               | List task files for pending/processing/completed/failed, or quarantined `corrupt/` files; cursor-free, `limit` query param (default 50, max 200)                                                                                                                                                                                                                                                                                               |
| POST /admin/tasks/:taskId/retry                  | retryTask       | admin.ts               | Requeue a permanently-failed task as a brand-new pending task (`requeueFailedTask`; a fresh ID is required, not reused)                                                                                                                                                                                                                                                                                                                        |
| DELETE /admin/tasks/:status/:fileName            | deleteTask      | admin.ts               | Delete a task file from pending/, failed/, or corrupt/ (processing/ and completed/ are excluded — worker-owned / retained history)                                                                                                                                                                                                                                                                                                             |
| GET /admin/branch-health                         | branchHealth    | admin-branch-health.ts | Runs `scanBranchHealth()` (branch-health.ts) over the configured branches root; returns `{ entries, generatedAt }`                                                                                                                                                                                                                                                                                                                             |
| POST /admin/branch-dirs/:dirName/purge           | purgeBranchDir  | admin-branch-health.ts | Reversibly renames a corrupt-metadata or orphan branch dir to `.trash-{dirName}-{STAMP}` (never deletes). Base branch is never a valid target; server re-derives live/corrupt/orphan state itself; a fresh (<5min) provisioning lock or a young (<15min) orphan blocks with 409; runs under a double lock (zero-retry provisioning lock + branch.json OCC lockfile) to avoid racing a concurrent repair-metadata call. Returns `{ trashedAs }` |
| POST /admin/branch-dirs/:dirName/repair-metadata | repairBranchDir | admin-branch-health.ts | For a corrupt (not missing, not healthy) branch.json: archives it to `branch.json.corrupt-{STAMP}` then recreates default metadata via the normal `BranchMetadataFileManager.save()` path. Unlike purge, the base branch IS a valid target (recovery path for a corrupt base-branch metadata file). Returns `{ branch, archivedAs }`                                                                                                           |

**API Support Files**:

| File                | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| route-builder.ts    | Declarative route builder with Zod validation, declarative guards, and code generation metadata                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| guards.ts           | Declarative guard system (branch access, schema loading, role checks) for route-builder; `writableBranch` rejects 403 when the branch is the read-only protected base branch (prod only) OR when branch status is not `'editing'` (submitted/approved/locked/archived — both modes); wired into content write/rename, entries delete, schema mutations. `submittableBranch` (403 on submit when the branch is the protected base branch — both modes). Both delegate protected-base checks to `getBranchProtection` (authorization/protected-branch.ts) |
| github-sync.ts      | GitHub sync helpers (submit PR, convert to draft) - delegates to githubService or task queue                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| settings-helpers.ts | Settings branch context resolution and commit helpers for permissions/groups                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| validators.ts       | Zod schemas for branded types at API boundaries; `queryBooleanSchema` for boolean GET query params (accepts booleans and 'true'/'false' strings; deliberately not z.coerce.boolean(), which coerces 'false' to true)                                                                                                                                                                                                                                                                                                                                    |
| types.ts            | ApiContext, ApiRequest, ApiResponse types                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| client.ts           | Generated API client                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

**Key Types**: ApiContext (services, user, branch), ApiRequest, ApiResponse

### API Middleware

**Location**: packages/canopycms/src/api/middleware/

| Middleware       | Purpose                                                     |
| ---------------- | ----------------------------------------------------------- |
| branch-access.ts | Branch access guards (guardBranchAccess, guardBranchExists) |

**Pattern**: Use `guardBranchAccess()` for combined branch existence + user access check, or `guardBranchExists()` when doing content-level permission checks later.

```typescript
const result = await guardBranchAccess(ctx, req, params.branch)
if (isBranchAccessError(result)) return result
const { context } = result
```

**Git Operations Pattern**: API handlers use service methods for git operations:

- permissions.ts, groups.ts -> Use `ctx.services.commitFiles()` for admin config changes
- branch-status.ts -> Uses `ctx.services.submitBranch()` for full submit workflow
- github-sync.ts -> Delegates to githubService (direct) or task queue (async) depending on internet availability
- All handlers access paths via `context.branchRoot` and `context.baseRoot` (BranchContext extends BranchPaths)

## Authentication & Permissions

### Auth Module

**Location**: packages/canopycms/src/auth/

| File                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| plugin.ts                | AuthPlugin interface definition; `verifiesCredentials?: boolean` — affirmative allowlist marker for plugins that cryptographically verify credentials; `assertAuthPluginAllowedForMode(plugin, mode)` throws when `mode === 'prod'` and the plugin doesn't set `verifiesCredentials: true` (fail-closed allowlist; replaces the old `insecureDevOnly` denylist marker)                 |
| types.ts                 | CanopyUser, AuthPluginConfig, AuthenticationResult, UserSearchResult, GroupMetadata types                                                                                                                                                                                                                                                                                              |
| context-helpers.ts       | Auth context helper utilities (extractHeaders, isCanopyRequest)                                                                                                                                                                                                                                                                                                                        |
| caching-auth-plugin.ts   | CachingAuthPlugin - wraps token verifier with cached metadata lookups; optional lazyRefresher for auto-populating cache in dev; AuthCacheProvider interface; fourth ctor param `options?: { verifiesCredentials?: boolean }` forwards the wrapped inner plugin's affirmation (cannot launder an insecure plugin — the inner plugin is asserted BEFORE wrapping, in context-wrapper.ts) |
| file-based-auth-cache.ts | FileBasedAuthCache - reads JSON cache from EFS; writeAuthCacheSnapshot - atomic snapshot+symlink writes                                                                                                                                                                                                                                                                                |
| cache.ts                 | Server-only re-exports for `canopycms/auth/cache` import path                                                                                                                                                                                                                                                                                                                          |
| index.ts                 | Public exports (types only for cache; implementations via `canopycms/auth/cache`)                                                                                                                                                                                                                                                                                                      |

### Auth Plugin Verification Allowlist

Fail-closed guard (SEC-C1) against a dev/insecure auth plugin accidentally serving prod traffic. `AuthPlugin.verifiesCredentials?: boolean` (auth/plugin.ts) is an allowlist, not a denylist: a plugin must affirmatively set it `true` to be usable when `mode: 'prod'`. `assertAuthPluginAllowedForMode(plugin, mode)` throws otherwise, and is called against the plugin BEFORE any wrapping — e.g. by `createNextCanopyContext()` (canopycms-next/context-wrapper.ts) and `http/handler.ts` — so `CachingAuthPlugin` can't hide an unset marker. `ClerkAuthPlugin` (canopycms-auth-clerk) sets `verifiesCredentials = true` (real JWT verification); `DevAuthPlugin` (canopycms-auth-dev) deliberately leaves it unset so it fails closed if ever pointed at `mode: 'prod'`.

### Auth Cache System

For Lambda/production environments where the auth provider API is unreachable (no internet):

**Architecture**: Token verification is done locally (JWT). User/group metadata comes from JSON files on EFS populated by the EC2 worker.

| Component              | Location                      | Purpose                                                                                                                    |
| ---------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| AuthCacheProvider      | auth/caching-auth-plugin.ts   | Interface for any cache backend                                                                                            |
| CachingAuthPlugin      | auth/caching-auth-plugin.ts   | AuthPlugin impl: local token verify + cached metadata; optional lazyRefresher auto-populates cache on first request in dev |
| TokenVerifier          | auth/caching-auth-plugin.ts   | Function type: extract/verify token from request context                                                                   |
| FileBasedAuthCache     | auth/file-based-auth-cache.ts | Reads users.json, orgs.json, memberships.json from EFS                                                                     |
| writeAuthCacheSnapshot | auth/file-based-auth-cache.ts | Atomic write: timestamped snapshot dir + symlink swap                                                                      |

**Import paths**:

- Types: `import type { AuthCacheProvider, TokenVerifier } from 'canopycms/auth'`
- Implementations (server-only): `import { FileBasedAuthCache, CachingAuthPlugin } from 'canopycms/auth/cache'`

### Authorization Module

**Location**: packages/canopycms/src/authorization/

This module provides unified access control with a layered architecture:

| File                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| content.ts             | Main entry — combined branch + path access (`checkContentAccess` + `createContentAccessChecker`, see below)                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| branch.ts              | Branch-level access control (checkBranchAccessWithDefault); `canPerformWorkflowAction(context, user, defaultAccess, options?)` — submit/withdraw eligibility (creator OR ACL OR system-branch grant); `options.isProtectedBranch` (pass `getBranchProtection(...).isProtected`) disables the system-branch grant so only admins/reviewers/creator/ACL users keep workflow rights on the protected base branch                                                                                                                                                                            |
| protected-branch.ts    | `getBranchProtection(config, branchName)` -> `{ isProtected, submitBlocked, readOnly }` - single source of truth for protected-base-branch policy: `isProtected` when `branchName` (sanitized) matches `config.defaultBaseBranch`; `submitBlocked` mirrors `isProtected` (never submittable, both modes - submitting the base branch would push straight to itself); `readOnly` is `isProtected && mode === 'prod'` (dev needs the base branch editable). Re-exported from `authorization/index.ts`; do not re-derive the comparison elsewhere.                                          |
| path.ts                | Path-level permissions (checkPathAccess); `resolveDefaultPathAccess(defaultAccess, level)` resolves a `DefaultPathAccess` config value to an 'allow'/'deny' verdict for one `PermissionLevel` — used at the `no_rule_match` fallback in `checkPathAccess`; string form applies to every level, object form looks up the level with an absent level resolving to 'deny' (fail-closed); re-exported from `authorization/index.ts`                                                                                                                                                          |
| helpers.ts             | Utility functions (isAdmin, isReviewer, isPrivileged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| types.ts               | Type definitions (BranchAccessResult, ContentAccessResult)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| settings-file-store.ts | `mutateSettingsJsonFile(opts)` — shared cross-host layered-lock mutation helper (`withLock` → `withOccFileLock` → `withOccRetry`/`writeOccJsonFile`, see occ-json-write.ts) for settings JSON files (permissions.json, groups.json); `SettingsFileConflictError` (lock/retry exhausted — busy, retry) and `SettingsVersionConflictError` (caller's `mutate` detected an `expectedContentVersion` mismatch — real edit conflict, never retried); the old hand-rolled `version: 1` literal + separate `contentVersion` field are gone in favor of the OCC-managed `version`/`writeId` pair |
| permissions/           | Permissions file schema (Zod, `version`/`writeId` optional) and loader (`permissions/loader.ts`): `loadPermissionsFile`/`loadPathPermissions`; `mutatePermissionsFile(repoRoot, mode, mutate, options?)` (replaces the removed `savePathPermissions`, built on settings-file-store.ts); `ensurePermissionsFile` reimplemented as a no-op mutation on top of it                                                                                                                                                                                                                           |
| groups/                | Groups file schema (Zod, `version`/`writeId` optional) and loader (`groups/loader.ts`): `loadGroupsFile`/`loadInternalGroups`; `deriveInternalGroups(fileGroups, bootstrapAdminIds)` — pure reserved-groups-reconciliation logic extracted from `loadInternalGroups` for reuse inside mutators; `mutateGroupsFile(branchRoot, mode, mutate, options?)` (replaces the removed `saveInternalGroups`, built on settings-file-store.ts)                                                                                                                                                      |
| test-utils.ts          | `unsafeAsPermissionPath`; `createTestContentAccess(deps)` (builds both checkers from one deps object)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

**Usage Pattern**:

```typescript
import { checkContentAccess } from './authorization'

const result = await checkContentAccess(
  deps,
  context,
  branchRoot,
  'content/posts/post.mdx',
  user,
  'edit',
)
if (result.allowed) {
  // User can edit the file
}
```

**Batch primitive**: `createContentAccessChecker(deps, context, branchRoot, user)` (also exposed as `services.createContentAccessChecker(context, branchRoot, user)`) does the request-constant work once — branch access, settings-root resolution, and the permission-rule load — and returns a synchronous per-path checker `(relativePath, level) => ContentAccessResult` (type `ContentAccessChecker`). `checkContentAccess` delegates to it, so its signature and behavior are unchanged. Used by listing endpoints (`api/entries.ts`, `api/resolve-references.ts`, `api/reference-options.ts`) to avoid re-loading permissions per entry.

### Permission Model

- Groups-only (no roles) - users belong to groups with associated permissions
- Reserved groups: "Admins" (full access), "Reviewers" (can review/approve PRs)
- Path-based ACLs: Define who can edit specific files/trees
- Bootstrap admin: CANOPY_BOOTSTRAP_ADMIN_IDS env var

### Auth Flow

1. Host app provides getUser function to adapter
2. AuthPlugin.verifyUser validates external token
3. User mapped to CanopyUser with groups
4. Permission checks use path ACLs + group membership

**Production (Lambda) auth flow**:

1. CachingAuthPlugin verifies JWT locally (TokenVerifier)
2. User/group metadata looked up from FileBasedAuthCache (EFS JSON files)
3. Cache is populated externally by EC2 worker (refreshClerkCache or refreshDevCache)

### canopycms-auth-dev Package

**Location**: packages/canopycms-auth-dev/src/

| File            | Purpose                                                                                                                                                                                                                                                            |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| dev-plugin.ts   | DevAuthPlugin - mock users/groups for development; DEFAULT_USERS, DEFAULT_GROUPS; deliberately does NOT set `verifiesCredentials` (trusts headers/cookies with no cryptographic verification), so `assertAuthPluginAllowedForMode` rejects it under `mode: 'prod'` |
| cookie-utils.ts | Dev user cookie extraction                                                                                                                                                                                                                                         |
| jwt-verifier.ts | createDevTokenVerifier - extracts userId from headers/cookies for CachingAuthPlugin in dev mode                                                                                                                                                                    |
| cache-writer.ts | refreshDevCache - writes dev users/groups to EFS-style cache for FileBasedAuthCache                                                                                                                                                                                |
| client.ts       | Client-side components (UserSwitcherModal, UserSwitcherButton)                                                                                                                                                                                                     |
| index.ts        | All public exports                                                                                                                                                                                                                                                 |

### canopycms-auth-clerk Package

**Location**: packages/canopycms-auth-clerk/src/

| File            | Purpose                                                                                                                                                                                                                                                                                                                                        |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| clerk-plugin.ts | ClerkAuthPlugin - `verifiesCredentials = true` (real JWT verification); CLERK_SECRET_KEY + the Clerk client are resolved lazily and memoized on FIRST authenticated call, not in the constructor, so a zero-editor static build can import the plugin without the secret set; `verifyTokenOnly()` (networkless via `jwtKey`) stays secret-free |
| jwt-verifier.ts | createClerkJwtVerifier - networkless JWT-only TokenVerifier; @deprecated in favor of `ClerkAuthPlugin.verifyTokenOnly()`, which `createNextCanopyContext()` wires into CachingAuthPlugin automatically                                                                                                                                         |
| cache-writer.ts | refreshClerkCache - populates FileBasedAuthCache from the Clerk API (users/orgs/memberships)                                                                                                                                                                                                                                                   |
| client.ts       | `useClerkAuthConfig()` - wires Clerk's `UserButton`/`signOut` into the editor's `AccountComponent`/`onLogoutClick`                                                                                                                                                                                                                             |
| index.ts        | Public exports                                                                                                                                                                                                                                                                                                                                 |

## Worker Module

**Location**: packages/canopycms/src/worker/

The CMS Worker daemon handles operations that Lambda (with no internet) cannot perform.

| File                 | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| cms-worker.ts        | CmsWorker class - main daemon with task processing, git sync, auth cache refresh; builds its Octokit instance via `createCanopyOctokit()` (github-service.ts) and shares `createOrUpdatePullRequest()` with `GitHubService` for the `push-and-create-or-update-pr` task; `CmsWorkerConfig.deploymentName` (optional) is resolved once in the constructor into `this.settingsBranch` (`canopycms-settings-${deploymentName ?? 'prod'}`) — see Operating Mode Module's Settings Branch Namespacing |
| task-queue.ts        | CMS-specific task queue wrapper with TaskAction types; re-exports `requeueFailedTask`/`listCorruptTaskFiles`/`CorruptTaskFile` from `task-queue/task-queue.ts` for `api/admin.ts`                                                                                                                                                                                                                                                                                                                |
| task-queue-config.ts | getTaskQueueDir() - resolves .tasks/ directory per operating mode                                                                                                                                                                                                                                                                                                                                                                                                                                |
| worker-status.ts     | `WORKER_STATUS_FILE` (`'worker-status.json'`) + `writeWorkerStatus(taskDir, report)` — the worker daemon's self-reported liveness snapshot (`{taskDir}/worker-status.json`; wire type `WorkerStatusReport` in `../types`); single-writer, full-regeneration atomic write (never read-modify-write, so concurrent writers during a lock-compromise window can't corrupt it); read tolerantly by `api/admin.ts`'s `readWorkerStatus`                                                               |

**CmsWorker responsibilities**:

- Process queued tasks from Lambda (push branches, create/update PRs, convert to draft, close PRs, delete remote branches)
- Sync bare repo (remote.git) with GitHub on a schedule; each cycle also pushes THIS deployment's own settings branch (`pushSettingsBranches()`, private) — narrowed to `this.settingsBranch` only (previously pushed every local `canopycms-settings-*` branch); any OTHER such branch found locally (reconciled in via the tracking-namespace fetch, signalling another deployment shares this repo) is warned about but never pushed; the swallowed-error warning is more specific when the swallowed error is itself a non-fast-forward rejection (`isNonFastForwardRejection`, utils/git.ts)
- `pushBranchToGitHub` (private) classifies push failures via `isNonFastForwardRejection` (utils/git.ts) and throws `PermanentTaskError` (fail-fast, skips the task's retry budget) on a non-fast-forward rejection — the branch moved on GitHub (another deployment sharing the repo, or a direct push) and retrying the identical push can never succeed; requires stable English git output, so the push's simple-git instance is configured with `gitChildEnv({})` (git-manager.ts, forces `LC_ALL=C`/`LANG=C`)
- `updateBranchMetadataOnFailure` records the (already-redacted) failure reason into `BranchMetadata.syncFailureReason` alongside `syncStatus: 'sync-failed'`, so the editor can show WHY a sync failed, not just that it failed; `updateBranchMetadata` (success path) explicitly clears `syncFailureReason` on the next successful sync (a stale reason would otherwise survive forever, since `save()`'s merge only overwrites keys present in the update)
- Fast-forward the base branch's own workspace clone (`content-branches/<base>`) to `origin/<base>` each sync cycle (`refreshBaseBranchWorkspace()`, private) — ff-only (a non-fast-forward is treated as should-never-happen and left untouched); bumps content-index caches on fast-forward; clears stale conflictStatus/conflictFiles left over from before the base branch was excluded from the rebase loop
- Poll GitHub for `submitted`/`approved` branches' PR resolution (`pollMergeState()`, private) — merged branches auto-archive via `buildMergedBranchUpdate` (shared with the manual `markAsMerged` API); closed-without-merge branches get `pullRequestState: 'closed'` recorded only (no status change — an admin decides next steps); best-effort, failures logged and retried next cycle
- Rebase active branch workspaces when upstream changes (resolve-and-continue strategy with ContentId-based conflict tracking)
- Sweep expired `.trash-*` branch directories once per `syncGit()` cycle (`cleanupTrashedBranchDirs()`, private, called alongside the existing `cleanupOldTasks()`) — removes dirs the admin purge endpoint (`api/admin-branch-health.ts`) renamed to trash more than 30 days ago; age comes only from the directory name's embedded timestamp, never mtime (rename preserves the source's original mtime); names that don't match the expected `.trash-{dirName}-{STAMP}` shape are left alone and logged
- Refresh auth metadata cache (pluggable callback)

**Rebase conflict handling** (`rebaseActiveBranches()`):

1. Skips the base branch's own workspace dir (refreshed separately by `refreshBaseBranchWorkspace()`, not rebased) and branches with uncommitted changes (dirty tree); previously-silent skips are now logged
2. For branches that are `submitted`/`approved` (in review, not rebased under an active PR): still polls GitHub for PR resolution via `pollMergeState()` before skipping, since nothing else signals a merge/close back to the worker
3. On conflict: applies `--theirs` (the branch's version in rebase context) to keep editor work, then continues
4. Records conflicting files as `ContentId[]` in `BranchMetadata.conflictFiles`
5. For `.collection.json` files: extracts ContentId from the parent directory name; falls back to `ROOT_COLLECTION_ID` for the root content directory
6. Non-entry files with no extractable ContentId are excluded from `conflictFiles`
7. Test file: `worker/cms-worker-rebase.test.ts` (11 integration tests using real git)

**Task Actions** (TaskAction type):

| Action                       | Purpose                               |
| ---------------------------- | ------------------------------------- |
| push-branch                  | Push a branch to GitHub               |
| push-and-create-pr           | Push + create new PR                  |
| push-and-update-pr           | Push + update existing PR             |
| push-and-create-or-update-pr | Push + idempotent PR create-or-update |
| convert-to-draft             | Convert PR to draft (via GraphQL)     |
| close-pr                     | Close a PR                            |
| delete-remote-branch         | Delete a remote branch ref            |

**Worker lock**: Uses EFS-based lock file (.tasks/.worker-lock) with atomic O_CREAT|O_EXCL to prevent concurrent workers.

## Task Queue Module

**Location**: packages/canopycms/src/task-queue/

Generic file-based persistent task queue with zero Canopy dependencies.

| File          | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| task-queue.ts | Core queue operations: enqueue, dequeue, complete, fail, retry, recover, cleanup, query; `requeueFailedTask(taskDir, taskId)` — reads failed/{taskId}.json and creates a brand-new pending task (fresh id, `payload.requeuedFrom` provenance; write-then-unlink ordering so a crash mid-op never loses the task); `listCorruptTaskFiles(taskDir, limit?)` — surfaces quarantined corrupt/ files (`listTasks()` silently drops unparseable ones) |
| types.ts      | Task, TaskStatus, QueueStats (incl. `corrupt` count), TaskQueueLogger, `CorruptTaskFile` (fileName, size, mtime, rawSnippet) interfaces                                                                                                                                                                                                                                                                                                         |
| index.ts      | Public re-exports                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Directory structure** on filesystem:

```
.tasks/
  pending/      -- ready to be picked up
  processing/   -- currently being executed
  completed/    -- finished successfully
  failed/       -- permanently failed (exhausted retries)
  corrupt/      -- unreadable files moved here for inspection
```

**Key features**:

- FIFO ordering by createdAt with stable tiebreaker
- Retry with exponential backoff (5s -> 10s -> 20s -> 40s -> 60s cap)
- Crash recovery: recoverOrphanedTasks moves stale processing/ tasks back to pending/
- Deduplication: skips tasks that already exist in completed/ or failed/
- Auto-cleanup: completed/failed tasks deleted after 30 days

## CLI Module

**Location**: packages/canopycms/src/cli/

Bootstrapping scripts run via `pnpm exec canopycms <command>`. Uses `@clack/prompts` for interactive CLI experience.

| File                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cli.ts                 | CLI entrypoint: minimist-based arg parsing, command routing, `isDirectRun` guard; dynamically imports command modules; `resolveSyncSubcommand()` maps positional arg to sync subcommand; project-scoped commands (sync, migrate, generate-ai-content, worker run-once) resolve the project root via `requireProjectRoot()` (project-root.ts)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| init.ts                | Library functions: `init()`, `initDeployAws()`, `workerRunOnce()` (no CLI logic, imported by cli.ts); exports `AuthProvider` type (`'clerk' \| 'dev'`), `InitOptions` (includes `authProvider?`, `staticBuild?` for non-interactive use); with `staticBuild`, the generated config sets `deployedAs` from `CANOPY_BUILD` (`'static'` for the public export, else `'server'`) and the AI route uses the `.server.ts` route extension                                                                                                                                                                                                                                                                                                                                 |
| sync.ts                | Interactive CLI wrapper for bidirectional content sync between working tree and branch workspaces; push copies+commits to workspace, pull copies back, both does 3-way git merge via canopycms-sync-base tag, abort cancels a failed merge. The prompt-free copy/diff/commit primitives (`assertWithinDir`, `safeReplaceDir`, `pushContentToWorkspace`, `SYNC_BASE_TAG`, etc.) now live in `src/sync-core.ts` and are imported here; this file adds `selectBranch` interactive branch picker (accepts `autoCreate` to auto-create workspaces on push) and `detectCurrentBranch` helper; exports `SyncError` — preconditions (missing content dir, unknown branch) throw instead of warn+exit-0, and content validation runs before workspace auto-creation          |
| migrate.ts             | `canopycms migrate` command: converts a plain content tree to CanopyCMS conventions — ID-based entry renames (`{type}.{slug}.{id}.{ext}`), directory renames with embedded IDs, `.collection.json` scaffolding; idempotent (already-conforming names skipped), `--dry-run` supported; exports `migrate()`, `MigrateError`, `slugifyName`, `MIGRATE_FORMATS` (`md`/`mdx`/`json`/`yaml`); when `projectDir` is a branch clone (detected via `BranchMetadataFileManager.loadOnly`), the rename/write ops run under the SAME `{branchRoot}/.canopy-meta/schema` surrogate lock SchemaOps uses (`withSchemaLock`, schema/schema-store.ts) so a migrate run can't interleave with a concurrent live schema edit; a plain (non-clone) project root skips the lock entirely |
| project-root.ts        | `findProjectRoot(startDir)` + `PROJECT_MARKER` (`canopycms.config.ts`): walks up from cwd to the nearest config file (like git's `.git` discovery) so CLI commands work from subdirectories without scattering state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| generate-ai-content.ts | AI content generation CLI command                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| templates.ts           | Template generators with auth-provider-specific and static-build-specific output; templates accept `{ authProvider, staticBuild }` options to tailor canopy context, edit page, next.config, and middleware files; includes `middleware()` export                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| template-files/        | Template files for scaffolding (config, route, edit page, middleware, next.config.ts, AI content endpoint, Dockerfile, CI workflow); canopy.ts template exports `getCanopy`, phase-selecting `readByUrlPath`/`read`, a bound `contentStaticParams` helper (for `generateStaticParams`), `getHandler`, and `getCanopyForBuild` (demoted to an advanced escape hatch — ACL-bypassing admin context); next.config templates import `withCanopy` from `canopycms-next/config` (CJS-compatible entry point); templates generated dynamically based on `authProvider` and `staticBuild` options                                                                                                                                                                           |

**Commands**:

| Command                         | Purpose                                                                                                                                            |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `canopycms init`                | Scaffold CanopyCMS into a Next.js app (config, API route, edit page, middleware, schemas, AI content endpoint, next.config.ts with `withCanopy()`) |
| `canopycms init-deploy aws`     | Generate AWS deployment artifacts (Dockerfile.cms, GitHub Actions workflow)                                                                        |
| `canopycms worker run-once`     | Process pending tasks, refresh auth cache, then exit                                                                                               |
| `canopycms generate-ai-content` | Generate static AI-ready content files (default output: public/ai)                                                                                 |
| `canopycms sync <subcommand>`   | Content sync between working tree and branch workspaces (subcommands: push, pull, both, abort)                                                     |
| `canopycms migrate`             | Convert an existing plain content tree to CanopyCMS conventions (ID-based renames, `.collection.json` scaffolding; idempotent)                     |

**`init` flags**: `--app-dir <path>`, `--no-ai`, `--force`, `--non-interactive`, `--auth-provider <clerk|dev>`, `--static-build`

**`init` interactive prompts** (skipped with flags or `--non-interactive`):

1. Auth provider selection (Clerk + dev auth, or dev-only)
2. Dual-build / static build support (yes/no)

**`generate-ai-content` flags**: `--output <dir>`, `--config <path>`, `--app-dir <path>` (locates schemas.ts; default: `app`)

**`sync` subcommands**: `push` (push working-tree content to a branch workspace), `pull` (pull content from a branch workspace), `both` (3-way git merge via `canopycms-sync-base` tag), `abort` (cancel a failed merge in a branch workspace). Bare `canopycms sync` with no subcommand prints help. **`sync` options**: `--branch <name>` (target branch workspace), `--content-root <path>` (default: `content`), `--force` (skip confirmation prompts). Sync does not interact with remote.git.

**`migrate` flags**: `--content-root <path>` (default: `content`), `--entry-type <name>` (e.g. `doc`), `--format <md|mdx|json|yaml>`, `--schema <key>` (entry schema registry key), `--dry-run` (print plan only), `--force` (skip confirmation)

## CDK Package (canopycms-cdk)

**Location**: packages/canopycms-cdk/

AWS CDK constructs for deploying CanopyCMS to AWS.

| File                               | Purpose                                                                                                                                                                                                                                                                                        |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| src/constructs/cms-service.ts      | CanopyCmsService construct (VPC, EFS, Lambda, EC2 worker ASG); `CanopyCmsServiceProps.deploymentName?` (default `'prod'`) — namespaces the settings branch so two stacks can share one GitHub repo; stamped into the Lambda's `CANOPYCMS_DEPLOYMENT_NAME` env var and the worker's `.env` file |
| src/constructs/cms-distribution.ts | CanopyCmsDistribution construct (CloudFront, ACM cert, Route53 records)                                                                                                                                                                                                                        |
| worker/index.ts                    | EC2 worker entrypoint - reads Secrets Manager, wires Clerk auth cache, starts CmsWorker; reads `CANOPYCMS_DEPLOYMENT_NAME` from its own env (stamped by `CanopyCmsServiceProps.deploymentName`) and passes it through as `CmsWorkerConfig.deploymentName`                                      |

**CanopyCmsService creates**:

- VPC (2 AZs, public + private subnets, no NAT)
- EFS filesystem with access point at /workspace
- Lambda function (Docker image, EFS mount, private subnet, no internet)
- Lambda Function URL (for CloudFront origin)
- EC2 Worker (t4g.nano spot in ASG, public subnet, EFS mount, systemd service)
- CloudWatch log group (`workerLogGroup`) for worker stdout/stderr, shipped via the amazon-cloudwatch-agent (file-based `/var/log/canopy-worker/worker.log`, not journald); worker IAM role gets a scoped `CreateLogStream`/`PutLogEvents` grant. Configurable via `workerLogRetention` (default `THREE_MONTHS`) and `workerLogGroupName` (default `/canopycms/<stackName>/worker`)

**`deploymentName` prop** (`CanopyCmsServiceProps.deploymentName?`, default `'prod'`): both the Lambda and the EC2 worker resolve their settings branch through `resolveDeploymentName` (`packages/canopycms/src/operating-mode/deployment-name.ts`), which lets this env-stamped value win over any `deploymentName` baked into the shared repo's `canopycms.config.ts` — the point of this prop. Two `CanopyCmsService` stacks pointed at the SAME GitHub repo MUST set distinct values, or both resolve to `canopycms-settings-prod` and fight over the same branch. Changing this value later, on a stack with an already-populated settings workspace, is refused at boot rather than silently reset (see settings-workspace.ts's rename guard) — plan the value up front for any stack sharing a repo.

**CanopyCmsDistribution creates** (optional):

- ACM certificate (DNS validated)
- CloudFront distribution with Function URL origin
- Route53 A/AAAA alias records

## canopycms-next Package

**Location**: packages/canopycms-next/src/

Next.js-specific adapter layer. Provides the catch-all API handler, context creation, a Next.js config wrapper, client components, and test utilities.

| File               | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| with-canopy.ts     | `withCanopy()` Next.js config wrapper: auto-detects installed Canopy packages via `require.resolve` (only `canopycms` required; optional packages like `canopycms-next`, `canopycms-auth-clerk`, `canopycms-auth-dev`, `canopycms-cdk` added only if installed), transpilePackages + React dedup aliases (webpack) + dual-build page extensions (see below); `staticBuild: true` adds `STATIC_PAGE_EXTENSIONS` (`static.ts`/`static.tsx`) to `pageExtensions` instead of `CMS_PAGE_EXTENSIONS` (`server.ts`/`server.tsx`) — enables a split-page dual-build convention where `page.static.tsx` prerenders (`dynamicParams = false`, required by `output: 'export'`) and `page.server.tsx` is `force-dynamic` with no `generateStaticParams` (path ACLs apply, unknown slugs 404 instead of Next's internal NoFallbackError)                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| adapter.ts         | `createCanopyCatchAllHandler()`, `wrapNextRequest()` for Next.js catch-all API route                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| context-wrapper.ts | `createNextCanopyContext()` returns `NextCanopyContextResult` with: `getCanopy` (request-scoped via React `cache()` + `headers()`), `getCanopyForBuild` (returns `CanopyBuildContext` — narrower type, no `user`; now surfaces build-safe `read`/`readByUrlPath` alongside buildContentTree/listEntries/services; uses `STATIC_DEPLOY_USER`, memoized per process, safe for `generateStaticParams`/`generateMetadata`; guarded by `guardBuildContext`), phase-selecting `readByUrlPath`/`read` (build context at build via `isBuildMode()`, runtime context at request — recommended for `[...slug]`/`[slug]` page resolution), a bound `generateContentStaticParams(options)` (enumeration-only; closes over the build context via `collectStaticParams` so page modules never import the admin context), `handler` (API catch-all), `services`. Starts the dev content watcher (`startDevContentWatcher`) when mode === 'dev' (not in build mode). Exports `guardBuildContext(buildCtx, config)` (also used by tests): wraps build-context reads to throw only when `mode === 'prod' && deployedAs === 'server' && !isBuildMode()` (prod-only — Next runs legit static hooks in `next dev` with the same signature; `static` deployments skip ACLs anyway) |
| static.ts          | `collectStaticParams(buildCtx, options)` — Next adapter over `collectStaticPaths` (enumeration-only: takes a build context with `listEntries`, never reads entry content); emits the array Next's `generateStaticParams` expects. `GenerateContentStaticParamsOptions extends CollectStaticPathsOptions` (so `rootPath`/`filter` flow through) and adds `shape: 'catch-all'` (default, param = URL `segments`) or `'single'` (param = entry `slug`, pair with `rootPath` to scope a collection); `paramName` (default `'slug'`); `basePath` to scope + relativize `segments` for a nested catch-all route (e.g. `'/docs'` for `app/docs/[[...slug]]`). Exported from index.ts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| client.tsx         | `NextCanopyEditorPage` - client component that reads URL search params automatically                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| config.ts          | CJS-compatible entry point re-exporting `withCanopy` and `WithCanopyOptions` from `with-canopy.ts`; bundled via esbuild into both ESM (`dist/config.mjs`) and CJS (`dist/config.cjs`) for Next.js 13/14 config loading compatibility. Always uses bundled output (dev and published). Import path: `canopycms-next/config`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| test-utils.ts      | `createMockAuthPlugin()`, `createRejectingAuthPlugin()` for tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

**Entry Points**:

| Import path             | Format  | Purpose                                                                   |
| ----------------------- | ------- | ------------------------------------------------------------------------- |
| `canopycms-next`        | ESM     | Main exports (adapter, context wrapper, test utils)                       |
| `canopycms-next/client` | ESM     | Client components (`NextCanopyEditorPage`)                                |
| `canopycms-next/config` | ESM+CJS | `withCanopy` only -- CJS-compatible for `next.config.ts` in Next.js 13/14 |

### Dual-Build Support (`withCanopy` staticBuild option)

`withCanopy()` supports a dual-build convention where CMS-only files use `.server.ts` / `.server.tsx` extensions (e.g., `route.server.ts`, `page.server.tsx`), and static-export-only page variants use `.static.ts` / `.static.tsx` (e.g., `page.static.tsx`).

| `staticBuild` value | Behavior                                                                                                                                                                                                    |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `false` (default)   | Adds `server.ts` and `server.tsx` (`CMS_PAGE_EXTENSIONS`) to Next.js `pageExtensions`, so `.server.ts`/`.server.tsx` files are processed (API routes, editor page, and a route's server-build page variant) |
| `true`              | Adds `static.ts` and `static.tsx` (`STATIC_PAGE_EXTENSIONS`) instead, so Next.js processes a route's static-export-only page variant (e.g. `page.static.tsx`) and ignores `.server.*` CMS-only files        |

**Convention**: CMS-only routes (catch-all API handler, editor page) use `.server.ts`/`.server.tsx` extensions. This lets a single repo produce two build outputs: a full CMS build (dev/server) and a static export with zero editor code.

**Split-page content routes**: a content route can also ship two page variants for the same URL — `page.static.tsx` prerenders at build time (`export const dynamicParams = false`, required by `output: 'export'`) and `page.server.tsx` renders per-request (`export const dynamic = 'force-dynamic'`, no `generateStaticParams`) so path ACLs apply and unknown slugs 404 instead of throwing Next's internal `NoFallbackError`. Because Next.js statically parses route-segment config, a single shared page file cannot switch these behaviors on an env var — hence the two file variants, selected by which one `pageExtensions` includes for a given build.

```typescript
const isCmsBuild = process.env.CANOPY_BUILD === 'cms'
export default withCanopy(
  { output: isCmsBuild ? 'standalone' : 'export' },
  { staticBuild: !isCmsBuild },
)
```

## Comment System

**Location**: packages/canopycms/src/

### Comment Store

- comment-store.ts - Comment persistence and retrieval; writes go through layered concurrency control — in-process `withLock`, server-enforced `withOccFileLock`, and OCC (`version`/`writeId`) — via the shared `utils/occ-json-write.ts` helper (also used by branch-metadata.ts); see docs/concurrency.md

### Comment Types

- **Field comments**: Attached to specific form fields (canopyPath)
- **Entry comments**: General feedback on entire entry
- **Branch comments**: Discussion about the branch/PR

### Storage

- File: .canopycms/comments.json (per-branch, not committed)

## Assets Module

**Location**: packages/canopycms/src/assets/

Asset store v2: pluggable storage adapters (S3/local), an upload finalize pipeline (sniff/hash/dims/SVG-sanitize), and an on-demand image-transform engine reached via `/assets/t/{directives}/{hash32}/{slug}.{ext}` URLs. Design record: `.claude/future-tasks/assets-media-system.md`.

### Isomorphic vs. server-only split

Three files are deliberately import-chain-pure so they can be reached from client (editor) bundles and static builds without pulling in `node:crypto`, `sharp`, or the S3 SDK:

| File                    | Import surface                                                 | Notes                                                           |
| ----------------------- | -------------------------------------------------------------- | --------------------------------------------------------------- |
| asset-prefixes.ts       | Zero imports                                                   | `ASSET_PREFIXES`/`AssetPrefixes` bucket-prefix constants only   |
| transform-directives.ts | Zero imports                                                   | Pure parser/formatter for transform URLs                        |
| asset-url.ts            | asset-prefixes.ts + transform-directives.ts (both zero-import) | `assetUrl`/`assetSrcSet`, exported off the package's main entry |

Everything else in this directory (keys.ts, store-local.ts, store-s3.ts, factory.ts, pipeline.ts, finalize.ts, svg-sanitizer.ts, asset-src.ts, transform.ts, index.ts) is server-only (node:fs, node:crypto, sharp, @aws-sdk/client-s3, file-type, image-size, sanitize-html) and must never be imported from client/editor code. Client code needing only types should `import type` from types.ts directly.

### Key Files

| File                    | Purpose                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| types.ts                | `AssetStore`/`AssetMeta`/`StagedUploadTarget`/etc. contract; type-only, no runtime imports — safe for `import type` from client code                                                                                                                                                                                                                              |
| asset-prefixes.ts       | `ASSET_PREFIXES`/`AssetPrefixes` — the 5 bucket-prefix strings (originals/staging/meta/public/transform); split out of keys.ts (zero imports) so isomorphic modules avoid keys.ts's `node:crypto`                                                                                                                                                                 |
| keys.ts                 | Key/hash/slug helpers: `hashBytes`, `slugifyFilename`, `createKeyBuilders`, `originalKey`/`originalPrefix`/`stagingKey`/`metaKey`/`metaPrefix`/`publicKey`; re-exports `ASSET_PREFIXES`/`AssetPrefixes` from asset-prefixes.ts for existing importers                                                                                                             |
| store-local.ts          | `LocalAssetStore` — filesystem-backed adapter for dev/tests; mirrors the S3 store's prefix layout                                                                                                                                                                                                                                                                 |
| store-s3.ts             | `S3AssetStore` — S3-backed adapter; assumes an existing content bucket (versioning/SSE/replication configured by the site's CDK stack)                                                                                                                                                                                                                            |
| factory.ts              | `createAssetStore(media, opts)` — instantiates the configured store from `config.media` (`'s3'` / `'local'`; `'lfs'` reserved, not yet implemented); falls back to a local dev store when `media` is unset                                                                                                                                                        |
| pipeline.ts             | `runFinalizePipeline` — pure sniff/hash/dims/SVG-sanitize transform from raw upload bytes to `AssetMeta` (no store I/O); `ALLOWED_UPLOAD_CONTENT_TYPES`                                                                                                                                                                                                           |
| finalize.ts             | `finalizeAsset`/`finalizeStagedUpload` — store orchestration around the pipeline (dedup via `putMetaIfAbsent`, write, best-effort staging cleanup); validates staging keys match the `asset-staging/{uuid}` shape                                                                                                                                                 |
| svg-sanitizer.ts        | `sanitizeSvg` via `sanitize-html` (chosen over dompurify+linkedom, which was found to silently no-op and let `<script>`/`onload=` through)                                                                                                                                                                                                                        |
| asset-src.ts            | `assetSrc(meta)` — root-relative public URL for an asset's meta (svg/pdf → static `assets/{hash32}/{slug}.{ext}`; raster → `assets/t/orig/{hash32}/{slug}.{ext}`); re-exports `IDENTITY_TRANSFORM_DIRECTIVE`, whose definition now lives in transform-directives.ts                                                                                               |
| transform-directives.ts | Pure, dependency-free (zero imports) parser/formatter for `/assets/t/{directives}/{hash32}/{slug}.{ext}` transform URLs; `parseTransformPath`, `formatDirectives` (canonical key ordering: c,f,q,w), `isAllowedTransformWidth` (multiples of 160 in [160,4096]), `IDENTITY_TRANSFORM_DIRECTIVE` (`'orig'`); types `TransformDirectives`/`CropRect`/`OutputFormat` |
| transform.ts            | Server-only (imports `sharp`); `applyTransform(input: {data, ext}, directives)` — resize/crop/reformat/EXIF-strip pipeline (`.rotate()` bakes orientation into pixels; re-encodes through the source format when no `f=` is given, so identity requests still strip metadata); returns typed `TransformSuccess`/`TransformRejection`                              |
| asset-url.ts            | Pure, isomorphic (only imports asset-prefixes.ts + transform-directives.ts); `assetUrl(ref, opts)` / `assetSrcSet(ref, widths, opts)` — build/adjust transform URLs from host-app or editor code without pulling in sharp                                                                                                                                         |
| index.ts                | Internal server-side barrel re-exporting the full store/pipeline/transform surface (including transform-directives.ts/transform.ts/asset-url.ts) for server-side consumers; NOT a package entrypoint                                                                                                                                                              |

### Transform URL shape

`/assets/t/{directives}/{hash32}/{slug}.{ext}` — `{directives}` is either the literal `orig` (identity: EXIF-strip only, no resize/format/quality change) or comma-separated `key=value` pairs: `w=` (width, multiple of 160 in [160,4096]), `f=` (`webp`/`jpeg`/`png`, must match `{ext}` when present), `q=` (quality 1-100), `c=` (normalized crop rect `x:y:w:h`, four floats in [0,1]). Dev-mode lazy transform emulation — cache-miss under `assets/t/` → parse directives → load original via the asset store → `applyTransform` → write back under the canonical key → serve — lives in `api/assets.ts`'s internal `serveLazyTransform` helper, and is designed to be reused unchanged by the future prod transform Lambda.

### Isomorphic exports (package main entry)

`assetUrl`, `assetSrcSet`, and types `AssetRef`/`AssetUrlOptions`/`OutputFormat`/`CropRect`/`TransformDirectives` are re-exported from the package's main `index.ts` (client-safe, no `node:` imports) alongside the existing isomorphic exports (`sanitizeHref`, config types, etc.).

### Dependencies

`sharp` (^0.35.3) — runtime dependency of transform.ts only (packages/canopycms/package.json); not used by any isomorphic module in this directory.

## Content Store

**Location**: packages/canopycms/src/

### Key Files

| File                        | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| content-store.ts            | Content persistence (write operations); ContentStoreError with typed error codes for structural matching; `resolveReferencesInData` recurses through inline groups; `ContentStoreOptions` third constructor arg (`{ indexFreshnessIntervalMs }`); `idIndex()` probes the on-disk generation marker; `readById` self-heals stale lookups; `write()` cross-process existence guard can throw `ContentConflictError`; mutator lock keys are namespaced content-ID keys derived from an unlocked `buildPaths()` directory-scan pre-pass, then `buildPaths()` re-runs inside the lock |
| content-reader.ts           | Content reading                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| entry-schema.ts             | Entry schema definitions (defineEntrySchema, TypeFromEntrySchema, defineBlockTemplate for reusable block templates); DistributeBlockTemplate for block discriminated unions; resolvedSchema on InferableField for typed references                                                                                                                                                                                                                                                                                                                                               |
| entry-schema-registry.ts    | Entry schema registry for reusable field definitions; validates isTitle (string only) and isBody (at most one, markdown/mdx only)                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| content-id-index.ts         | Content ID indexing for lookups; tree traversal (getEntriesInCollectionTree) and global entry queries (getAllEntryLocations)                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| content-index-registry.ts   | In-process invalidation registry: mutation sites call `invalidateContentIndexesForRoot`; ContentStores self-register (WeakRef-held). In-process scope only                                                                                                                                                                                                                                                                                                                                                                                                                       |
| content-index-generation.ts | Thin ContentId-index wrapper over `resource-generation.ts`'s generic marker; `invalidateContentIndexesDurable(root)` (content-only) and `invalidateBranchContentCaches(root)` (bulk tree mutations — also bumps the schema marker) are the two mutation-site entry points                                                                                                                                                                                                                                                                                                        |
| resource-generation.ts      | Generalized on-disk generation-marker primitive (per-resource `{root}/.canopy-meta/{resource}.generation`, random token); `bumpResourceGeneration`/`readResourceGeneration`/`isGenerationCurrent`; underlies content-index-generation.ts, branch-registry.ts, branch-schema-cache.ts — see docs/concurrency.md                                                                                                                                                                                                                                                                   |
| reference-resolver.ts       | Reference field resolution; loadReferenceOptions(collections?, displayField, search?, entryTypes?) with subcollection tree traversal and entry type filtering                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Content Model

- **Collections**: Containers for entries (posts, authors)
- **Entry Types**: Define content structure within collections; `maxItems: 1` for single-instance entries
- **Fields**: text, select, reference, object, code, block, markdown, group. Reference fields scoped by optional `collections` and/or `entryTypes` (at least one required; validated by `ensureReferenceFieldsHaveScope`). `group` fields (InlineGroupFieldConfig) are visual-only — they group child fields in the editor UI without adding a data key; all child values are stored flat in the parent object.
- **Field flags**: `isTitle` (marks one string-type field per schema as the display title; validated at registry load time; used by `resolveEntryTitle` fallback chain in `utils/title-field.ts`), `isBody` (marks one markdown/mdx field per schema as the file's body content; at most one per schema; validated at registry load time via `utils/body-field.ts`)
- **Format**: MD/MDX/JSON/YAML with frontmatter (gray-matter for md/mdx); JSON and YAML are data-only formats (no body/frontmatter separation)

## Content Tree

**Location**: packages/canopycms/src/content-tree.ts, packages/canopycms/src/content-listing.ts

Builds a tree of content nodes from the schema and filesystem for adopter use cases: navigation menus, sitemaps, search indexes, breadcrumbs.

### Key Files

| File               | Purpose                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------- |
| content-tree.ts    | `buildContentTree()` function and `ContentTreeNode` / options types                                           |
| content-listing.ts | `listEntries()`, `listCollectionEntries()`, `sortByOrder()`, `parseTypedFilename()`, `readEntryData()` shared |

### BuildContentTreeOptions<T, TEntryTypes>

| Option    | Default                                                                | Description                                                         |
| --------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------- |
| rootPath  | contentRoot name                                                       | Starting collection path                                            |
| extract   | none                                                                   | Extract typed custom fields from raw data                           |
| filter    | none                                                                   | Exclude nodes (and descendants); runs after extract                 |
| buildPath | strips content root, collapses index entries, lowercases, prepends `/` | Custom URL path builder; default collapses index to parent path     |
| sort      | order array then alphabetical                                          | Custom sort for children at each level; fully replaces default sort |
| maxDepth  | unlimited                                                              | Max traversal depth                                                 |

Without `sort`: children are ordered by the collection's `order` array first, then remaining items alphabetically. With `sort`: the comparator fully replaces the default sort. It runs after `extract` and `filter`, so `fields` is available.

### ContentTreeExtractMeta<TEntryTypes>

The second argument passed to `extract(data, meta)`. `buildContentTree<T, TEntryTypes>` accepts an optional second type parameter; `TEntryTypes` defaults to a loose `Record<string, unknown>` shape so existing callers are unaffected. When supplied, both `meta.entryType` and `meta.indexEntry` become discriminated on the entry-type literal union, giving adopters narrowed access to `meta.indexEntry.data` after switching on `meta.indexEntry.entryType`.

Exported helper: `EntryTypeMap = Record<string, object>` — the documented shape adopters can reuse, but `TEntryTypes` is unconstrained so plain interfaces (without index signatures) work too. Recommended pattern: key `entrySchemaRegistry` by entry-type name and derive `EntryTypes` via `EntryTypesFromRegistry<typeof entrySchemaRegistry>` — single source of truth, zero parallel declarations. Per-schema aliases like `PartnerContent = EntryTypes['partner']` follow naturally. Adopters who can't (or won't) key the registry by entry-type name can still declare a parallel `MyEntries` interface manually using `TypeFromEntrySchema<typeof someSchema>` per entry — see README "Migrating from the schema-name-keyed registry".

| Field       | Type                                                                                            | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| kind        | `'collection' \| 'entry'`                                                                       | Node kind                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| logicalPath | LogicalPath                                                                                     | Logical CMS path for the node                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| entryType   | `keyof TEntryTypes & string` (default: `string`)?                                               | Entry type name (present when `kind === 'entry'`); narrows to a literal union when `TEntryTypes` is supplied                                                                                                                                                                                                                                                                                                                                                                                                                    |
| format      | ContentFormat?                                                                                  | Content format (present when `kind === 'entry'`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| indexEntry  | `{ entryType: K; format: ContentFormat; data: TEntryTypes[K] }?` (discriminated union over `K`) | The collection's `slug === 'index'` entry, if any. Populated only when `kind === 'collection'` AND such an entry exists. Represents the collection's "identity" under the directory-as-page pattern (e.g., `/data-catalog/<partner>/partner.index.yaml` IS the partner's metadata). Undefined for collections at the `maxDepth` cap. When `TEntryTypes` is supplied, narrowing on `indexEntry.entryType` discriminates `indexEntry.data` to the matching type. Default `TEntryTypes` keeps `data` as `Record<string, unknown>`. |

Order of operations inside `buildNode`: for collections, entries are fetched BEFORE `extract` is called, so `meta.indexEntry` is populated when present and filters that depend on `node.fields` derived from index data work as expected. `ContentTreeNode` itself does NOT carry `indexEntry` — it is only surfaced via `meta`.

```ts
extract: (data, meta) => {
  if (meta.kind === 'collection' && meta.indexEntry?.entryType === 'partner') {
    return { isFictional: Boolean(meta.indexEntry.data.isFictional) }
  }
  // ...
}
```

## Content Listing (Batch)

**Location**: packages/canopycms/src/content-listing.ts

`listEntries()` returns a flat array of all content entries across collections. Designed for `generateStaticParams`, search indexing, sitemaps, and similar batch use cases.

### ListEntriesItem\<T\> Key Fields

| Field        | Type         | Description                                                                                                                           |
| ------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| pathSegments | string[]     | URL path segments (content root stripped)                                                                                             |
| urlPath      | string       | URL-ready path with index entries collapsed (`/guides` not `/guides/index`; root index is `/`). Round-trip safe with readByUrlPath    |
| slug         | Slug         | Entry slug within its collection                                                                                                      |
| entryPath    | LogicalPath  | Logical CMS path                                                                                                                      |
| entryId      | ContentId    | 12-char Base58 from filename                                                                                                          |
| collectionId | ContentId?   | Collection's content ID                                                                                                               |
| data         | T            | Entry data (full raw or extract-transformed)                                                                                          |
| schema       | EntrySchema? | Field definitions for the entry's type, when resolvable; consumed by `findInvalidEntries`/`assertBuildEntriesValid` (static/index.ts) |

### ListEntriesOptions\<T\>

| Option   | Default          | Description                                           |
| -------- | ---------------- | ----------------------------------------------------- |
| extract  | none (full raw)  | Transform raw data; for md/mdx, raw.body has markdown |
| filter   | none             | Exclude entries (runs after extract)                  |
| rootPath | contentRoot name | Starting collection path (skips out-of-scope entries) |
| sort     | entryPath alpha  | Custom sort comparator; default sorts by entryPath    |

**Access patterns**: standalone function from `canopycms/server`, method on `CanopyContext`, types from `canopycms`.

**Note**: `readEntryData()` now includes markdown body content in `data.body` for md/mdx entries. YAML entries are parsed as data-only (no body). Logs a warning on read/parse failure instead of silently returning `{}`.

## Configuration Module

**Location**: packages/canopycms/src/config/

| File                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| types.ts               | TypeScript type definitions for all config; ReferenceFieldConfig has optional `collections` and optional `entryTypes`; includes `entryLinkUrl?: EntryLinkUrlResolver` callback; InlineGroupFieldConfig (`type: 'group'`) is a FieldConfig union member for visual-only field grouping; `DevConfig` (`{ contentSync?: DevContentSyncMode }`) and `DevContentSyncMode` (`'off' \| 'warn'`); `CanopyConfig.dev?` / `CanopyConfigInput.dev?` (dev-mode-only behavior, ignored when mode !== 'dev'); `ValidateEntryHook` / `ValidateEntryInput` / `EntryValidationIssue` and optional `CanopyConfig.validateEntry` save-time validation hook ('error' issues reject the save with 422, 'warning' issues are returned as `validationWarnings`); `mode: OperatingMode` is REQUIRED on both `CanopyConfig` and `CanopyConfigInput` — no default (SEC-C1 follow-up: a prod deploy that omits `mode` must fail validation rather than silently running dev-trusting auth semantics); `DefaultPathAccess = DefaultPathAccessLevel ('allow' \| 'deny') \| DefaultPathAccessLevels` (`Partial<Record<PermissionLevel, DefaultPathAccessLevel>>`) — `defaultPathAccess` on `CanopyConfig`/`CanopyConfigInput` can now be a single value for all levels (unchanged) OR a per-level object (e.g. `{ read: 'allow' }` for public read while edit/review stay deny); an omitted level in the object form resolves to `'deny'` (fail-closed) via `resolveDefaultPathAccess`; exported from `canopycms` via the config barrel |
| schemas/config.ts      | Zod schema for CanopyConfig; `devConfigSchema` (+ `devContentSyncSchema` = `z.enum(['off','warn'])`, defaults `'warn'`); `dev` is an optional field on the config schema; `mode: operatingModeSchema` (`z.enum(['prod','dev'])`) has NO `.default()` — omitting `mode` throws at parse time instead of silently resolving to `'dev'`; `defaultPathAccessSchema` = `z.union([defaultPathAccessLevelSchema, defaultPathAccessLevelsSchema]).default('deny')` (`defaultPathAccessLevelsSchema` is a strict `{ read?, edit?, review? }` object with no per-field defaults, so an omitted level stays `undefined` post-parse); the `.default('deny')` sits on the OUTER union only, so an omitted top-level `defaultPathAccess` still resolves to `'deny'` and doesn't short-circuit the object form's per-level fail-closed resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| schemas/field.ts       | Zod schemas for field types; referenceFieldSchema has optional `collections` and optional `entryTypes` arrays                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| schemas/collection.ts  | Zod schemas for collections/entry types                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| schemas/permissions.ts | Zod schemas for permissions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| schemas/media.ts       | Zod schema for media config                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| flatten.ts             | Schema flattening for O(1) lookups                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| validation.ts          | Config validation utilities; `ensureReferenceFieldsHaveScope` ensures reference fields have at least one of collections or entryTypes; `ensureNoGroupsInsideComplexFields` throws if a group appears inside an object or block field (groups are only valid at entry schema top level or nested inside other groups)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| helpers.ts             | defineCanopyConfig, composeCanopyConfig (composeCanopyConfig threads `dev` through from fragments)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### Schema Definition

```typescript
import { defineCanopyConfig } from 'canopycms/config'

defineCanopyConfig({
  contentRoot: 'content',
  mode: 'dev', // required — no default (SEC-C1)
  deployedAs: 'static', // or 'server' (default)
  // ...
})
```

**`mode` is required**: unlike `deployedAs`/`contentRoot`/etc., `mode` has no Zod default — a config that omits it fails validation immediately instead of silently defaulting to `'dev'` (SEC-C1 follow-up: the old default could let a misconfigured prod deploy run with dev-trusting, header-based auth semantics). Tests that don't care about the value should use `defineCanopyTestConfig`/`createTestServices` (`src/config-test.ts`), which default `mode: 'dev'` for terseness; `defineCanopyConfig` (production config authoring) has no such fallback.

### Deployment Mode (`deployedAs`)

| Value      | Default | Behavior                                                                    |
| ---------- | ------- | --------------------------------------------------------------------------- |
| `'server'` | Yes     | Normal: auth plugin required, permissions enforced per-request              |
| `'static'` | No      | No request context, no auth; `STATIC_DEPLOY_USER` used; permissions skipped |

Checked via `isDeployedStatic(config)` in `context.ts`, `content-reader.ts`, `branch-workspace.ts` (short-circuits workspace creation), and `ai/resolve-branch.ts` (returns cwd). `isBuildMode()` is kept as a safety net for build-phase edge cases in server deployments.

### Dev Content Sync (`dev.contentSync`)

Dev-mode-only setting controlling how the dev server surfaces working-tree `content/**` edits made outside the editor that diverge from the served branch clone. Warn-only (it never writes). Ignored when `mode !== 'dev'`.

| Value              | Behavior                                                                    |
| ------------------ | --------------------------------------------------------------------------- |
| `'warn'` (default) | Log a warning naming the diverged files (on startup and on content changes) |
| `'off'`            | No watcher, no warnings                                                     |

There is intentionally no auto-push mode: overwriting the branch clone from the working tree would silently clobber uncommitted editor "Save" state. Reconcile explicitly via `canopycms sync push` (interactive, conflict-aware).

Implemented by `startDevContentWatcher()` (src/dev-content-watcher.ts), wired once at dev startup by `createNextCanopyContext()` (canopycms-next) via `dev.contentSync` (skipped in build mode). Builds on sync-core.ts read-only primitives (`diffContentTrees`, `isContentTreeDiffEmpty`).

## Schema Module

**Location**: packages/canopycms/src/schema/

| File                  | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| meta-loader.ts        | Load .collection.json files from filesystem; extracts ContentId from directory names; rejects "body" field name in md/mdx schemas; collection `order` is optional in the file (defaults to `[]` = alphabetical)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| resolver.ts           | High-level schema resolution API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| schema-store.ts       | SchemaOps class - CRUD for collections, entry types, ordering. All 7 mutators (createCollection, updateCollection, deleteCollection, addEntryType, updateEntryType, removeEntryType, updateOrder) run their read-modify-write body (a private `*Inner` method) under a private `withSchemaLock` — `withLock` + `withOccFileLock` on a coarse per-branch surrogate path (`{branchRoot}/.canopy-meta/schema`, NOT inside `.collection.json`'s content tree — see the module doc comment for why). Exports `SchemaStoreBusyError` (lock/retry exhausted, or branch deleted out from under the call) — api/schema.ts maps it to 409. `updateEntryTypeInner`'s breaking-change usage guard (rejects format/schema changes on an entry type with existing entries) now lives inside the lock here (moved from api/schema.ts, closing a TOCTOU between the usage count and the write) |
| schema-store-types.ts | Types for schema store operations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| types.ts              | EntrySchemaRegistry, SchemaResolutionResult types                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

**Pattern**: Schema structure comes from .collection.json files (single source of truth), field schemas come from an entry schema registry for reusability.

```typescript
import { resolveSchema } from 'canopycms/schema'

const { schema, sources } = await resolveSchema(contentRoot, entrySchemaRegistry)
```

## Editor UI

**Location**: packages/canopycms/src/editor/

### Context Providers

**Location**: packages/canopycms/src/editor/context/

| Provider            | Purpose                                    |
| ------------------- | ------------------------------------------ |
| ApiClientProvider   | Dependency injection for API client        |
| EditorStateProvider | Loading states, modal states, preview data |

**Usage**:

```tsx
<ApiClientProvider>
  <EditorStateProvider>
    <Editor />
  </EditorStateProvider>
</ApiClientProvider>
```

### Custom Hooks

**Location**: packages/canopycms/src/editor/hooks/

| Hook                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| useBranchManager       | Branch switching and creation; `loadBranches()` adopts `defaultBranch` from the branches API when no branch is pinned; `currentBranch` lookup matches by exact name, then falls back to comparing sanitized forms (`sanitizeBranchName`) so a raw unsanitized name from a legacy deep link (e.g. `?branch=feature/x`) still resolves to the persisted `feature-x`; `BranchSummary` mirrors `syncFailureReason` from the branches API response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| useBranchActions       | Branch workflow actions (create, submit, withdraw, merge); after create, adopts the server-sanitized branch name from the response (server may rewrite e.g. `feature/x` → `feature-x`) for `setBranchName`/URL/switch, so the client never sticks on a name the server never saved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| useEntryManager        | Entry loading and saving; exports `listAllEntries(apiClient, branch)` — follows the entries pagination cursor (200/page, 50-page safety cap, dedupes by logicalPath) and returns `{ entries, truncated }`; `refreshEntries` uses it to load ALL entries (yellow notification if cap hit) and guards against stale concurrent refreshes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| useDraftManager        | Draft state persistence (localStorage); drafts are a content-ID-keyed overlay over server-loaded values (`effectiveValue = draft ?? loadedValue`; Editor.tsx's entry-load effect gates on `loadedValues`, not `drafts`, so a restored draft never skips the server fetch); a successful save DROPS the draft key instead of overwriting it (fixes phantom-dirty on fresh loads) and fires the optional `onSaved` callback (Editor.tsx uses it to refresh entries); discard-draft actions (`handleDiscardDrafts`/`handleDiscardFileDraft`) confirm via Mantine `modals.openConfirmModal` first; `fieldErrors` (per-field validation errors for the selected entry) is derived from an entry-keyed `errorState` (`{ entryId, errors }`), so a stale error from a previously-selected entry can never leak onto the newly-selected one; exports `shallowEqualRecord(a, b)` helper (bails an effect out with the same object reference when a recomputed error map is equal to the last one) |
| useSchemaManager       | Collection/entry-type schema mutations (create/update/delete collection, add/update/remove entry type, reorder, delete entry); methods return result objects — `SchemaOpResult` (`{ ok: true } \| { ok: false; error }`) or `CreateCollectionResult` (adds `data: { collectionPath, contentId }` on success) — instead of a bare boolean/null, so callers get a real error message instead of a hardcoded generic one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| useCommentSystem       | Comment CRUD operations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| useGroupManager        | Group management operations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| usePermissionManager   | Permission management operations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| useEditorLayout        | Editor panel layout state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| useUserContext         | Current user context                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| useUserMetadata        | User metadata fetching                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| useReferenceResolution | Resolve reference IDs to display values                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| useEntryLinkResolution | Resolve `entry:ID` patterns in preview data before PreviewFrame; client-side URL map from editor entries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### Conflict Notice

Conflict indicators appear at two levels:

- **Per-entry**: `FormRenderer` accepts a `conflictNotice?: boolean` prop that displays a non-blocking informational alert when the current entry has a content conflict with the base branch. `Editor.tsx` computes this by checking whether `currentEntry.contentId` appears in `currentBranch.conflictFiles`.
- **Per-collection (navigator)**: `EntryNavCollection` has an optional `conflictNotice?: boolean` field. When true, `EntryNavigator.tsx` renders an orange "conflict" badge next to the collection name. `Editor.tsx` computes this by matching each collection's `contentId` against `currentBranch.conflictFiles`.

`EditorCollection` now carries `contentId?: ContentId` (threaded from `FlatSchemaItem` via `buildEditorCollections` in `editor-config.ts`).

`EntryNavigator.tsx` accepts a `loading?: boolean` prop: when true and the tree is empty it shows a loading indicator instead of the "No content" empty state.

### Branch PR State (BranchManager)

**Location**: packages/canopycms/src/editor/BranchManager.tsx

`BranchSummary` carries `pullRequestState?: PullRequestState` and `mergedAt?: string` (mirrors `BranchMetadata`). The branch list renders a teal "Merged" badge when `status === 'archived' && mergedAt`, and colors the PR badge red with a "(closed)" suffix when `pullRequestState === 'closed'` (blue otherwise). `getBranchPermissions()` blocks `canWithdraw`/`canRequestChanges` when `pullRequestState === 'closed'` — a closed-without-merge PR can't be converted back to a draft — with tooltip text explaining why.

`BranchSummary` also carries `isProtected`/`readOnly` (mirrors the `BranchListItem` wire flags, see Git & Branch Management > Protected Base Branch). `getBranchPermissions()` folds `isProtected` into `canSubmit`/`isSystemBranch`/`canDelete` (never true for the protected base branch); the branch list shows a "protected" badge and, in the submit/withdraw button's tooltip, "The base branch cannot be submitted" when `isProtected && !isSubmitted` (the Submit button itself is hidden, not just disabled).

`BranchSummary` also carries `syncFailureReason?: string` (mirrors `BranchMetadata`). The red "Sync failed" badge (`data-testid="sync-failed-badge-{name}"`, shown when `syncStatus === 'sync-failed'`) tooltip shows `GitHub sync failed: ${syncFailureReason}` when the worker recorded a reason, falling back to generic text otherwise.

### Permission Manager

**Location**: packages/canopycms/src/editor/permission-manager/

| File                       | Purpose                    |
| -------------------------- | -------------------------- |
| types.ts                   | Permission tree types      |
| utils.ts                   | Permission utilities       |
| hooks/usePermissionTree.ts | Tree state management      |
| hooks/useGroupsAndUsers.ts | Groups/users data fetching |

### Group Manager

**Location**: packages/canopycms/src/editor/group-manager/

| File                            | Purpose                   |
| ------------------------------- | ------------------------- |
| types.ts                        | Group management types    |
| hooks/useGroupState.ts          | Group state management    |
| hooks/useUserSearch.ts          | User search functionality |
| hooks/useExternalGroupSearch.ts | External group search     |

### System Health Panel (Admin)

**Location**: packages/canopycms/src/editor/admin/

Admin-gated observability/recovery UI for the `api/admin.ts`/`api/admin-branch-health.ts` endpoints (PR-A1..A4, PR-U1) — the first admin-gated editor UI. Visibility is entirely the caller's responsibility: `Editor.tsx` renders/opens it only when `isAdmin(userContext?.groups)` (authorization/helpers.ts); the component itself does not re-check.

| File                  | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SystemHealthPanel.tsx | `SystemHealthPanel` — Mantine `Modal` with three tabs: Overview (worker liveness badge, last git-sync summary/errors, recent-fatal-error alert, task-queue counts), Tasks (per-status table with retry/delete actions, confirm-modal copy explaining each action's races/limitations), Branches (`branch-health.ts` entries table: healthy/corrupt-metadata/orphan rows with mark-merged/repair/purge actions; purge disabled while a provisioning lock is fresh or an orphan is too young). Branches tab's `sync-failed` status text tooltip shows `` `${syncFailureReason} — retry from the Tasks tab` `` when the worker recorded a reason, generic text otherwise |
| useSystemHealth.tsx   | `useSystemHealth({ isOpen })` hook — loads status/tasks/branchHealth on open and polls every 30s while open (cleared on close); exposes `retryTask`/`deleteTask`/`purgeDir`/`repairDir`/`markMerged` action helpers (each calls the API, shows a Mantine notification, then refreshes); `isRecentFatalError` flags a `lastFatalError` < 30min old (crash-loop detection) computed at fetch time, not render time                                                                                                                                                                                                                                                      |

### Patterns

- Use Mantine theme helpers from theme.tsx
- "use client" required for browser components
- Export client components via canopycms/client
- Draft state persists in localStorage per branch/entry
- Branch selection: `CanopyEditorPage`/`CanopyEditor` have no 'main' fallback — with no pinned branch (URL/config) the editor starts branchless and adopts the server's `defaultBranch` from the branches API (`useBranchManager.loadBranches()`); explicit URL/config pins are never overridden
- Relative timestamps: `editor/relative-time.ts` exports `formatRelativeTime(iso)` — "just now"/"Nm ago"/"Nh ago"/"Nd ago", locale date string past 7 days; invalid input is returned unchanged (never renders "Invalid Date"). Shared by BranchManager.tsx, CommentsPanel.tsx, comments/InlineCommentThread.tsx

**Fields**: packages/canopycms/src/editor/fields/

Notable field components:

| File                 | Purpose                                                                                                                                                                                                                                                                                     |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| InlineGroupField.tsx | Renders `type: 'group'` fields as bordered Mantine Paper containers; transparent to data path — reads/writes at parent scope; supports nesting                                                                                                                                              |
| StringListField.tsx  | Editor for `type: 'string', list: true` fields; Mantine `TagsInput` with `splitChars={[]}`/`allowDuplicates` (no comma-splitting, duplicates preserved, for faithful array round-tripping). Before this component existed, list-string fields fell through to the single-value `TextField`. |

`FormRenderer.tsx` dispatches `type: 'group'` fields to `InlineGroupField` in the outer render loop (bypassing the normal per-field path logic, since inline groups do not add a key to the data shape) and `type: 'string', list: true` fields to `StringListField`.

**Components**: packages/canopycms/src/editor/components/

### Entry Link Fields

**Location**: packages/canopycms/src/editor/fields/entry-link/

MDXEditor toolbar integration for inserting `entry:CONTENT_ID` links into markdown content.

| File                 | Purpose                                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------------------- |
| EntryLinkContext.tsx | React context providing `EntryLinkOption[]` to toolbar components; bridges editor entry list to MDXEditor |
| InsertEntryLink.tsx  | Toolbar button + searchable entry picker modal (Mantine Combobox); inserts `[Title](entry:ID)` markdown   |
| index.ts             | Barrel exports                                                                                            |

**Client exports** (via `canopycms/client`): `EntryLinkContext`, `useEntryLinkContext`, `EntryLinkOption`, `EntryLinkContextValue`

### Preview Bridge

**Location**: packages/canopycms/src/editor/preview-bridge.tsx (`'use client'`; exported via `canopycms/client`)

Editor↔preview iframe postMessage bridge. Message types: `canopycms:draft:update`, `canopycms:preview:focus`, `canopycms:preview:highlight`, `canopycms:preview:ready`, `canopycms:preview:error`.

| Export                                                        | Purpose                                                                                                                                                                                                                              |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PreviewFrame                                                  | Editor-side iframe wrapper: pins the preview origin from `src`, posts drafts/highlight to it, validates inbound ready/error messages by source + origin, exposes `onPreviewError`                                                    |
| useCanopyPreview                                              | Site-side convenience hook: draft `data`/`isLoading`, `highlightEnabled`, `fieldProps()` (data-canopy-path), `reportError(message\|null, fieldPath?)` for draft compile/render failures (pairs with PreviewFrame's `onPreviewError`) |
| usePreviewData / usePreviewHighlight / usePreviewFocusEmitter | Site-side primitives wrapped by useCanopyPreview                                                                                                                                                                                     |
| isTrustedEditorMessage / resolveMessageOrigin                 | Security helpers: resolve URLs to origins; trust check for inbound messages                                                                                                                                                          |

All site-side hooks accept optional `{ editorOrigin }`. **Security model**: site-side listeners attach only when framed; messages must come from the direct parent window with a matching origin (same-origin default, `editorOrigin` override) — enforced by `isTrustedEditorMessage()`; all outbound posts target a concrete origin, never `'*'`.

## Git & Branch Management

**Location**: packages/canopycms/src/

### Key Files

| File                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| git-manager.ts           | Wrapper around simple-git; static `initializeWorkspace()` resolves the fork point via `resolveBaseBranch` (utils/git.ts); dev simulated-remote seeding (private `pushBranchToLocalRemote`) with `subdirectory` pushes a single `git commit-tree` snapshot of `<baseBranch>:<subdirectory>` (replaced `git subtree split`, which was O(history) and split HEAD instead of baseBranch); working-tree mutations call `invalidateBranchContentCaches` (content-index-generation.ts — bumps both the content-index and schema markers, since a checkout/merge/rebase can pull in `.collection.json` changes) unless `GitManagerOptions.skipIndexMarker` (settings workspaces: in-process invalidation only); exported standalone `ensureGitExcludePattern(repoPath, pattern)` (extracted from instance `ensureGitExclude`); exported `gitChildEnv(overrides)` — env allowlist (PATH/HOME/USER/LANG/LC*\*/TZ/TMPDIR/GIT_AUTHOR*_/GIT*COMMITTER*_/GIT_TERMINAL_PROMPT/GIT_TRACE\*) that ALSO forces `LC_ALL=C`/`LANG=C` on every caller (not just GitManager's own `this.git`), so git's rejection text stays untranslated for `utils/git.ts`'s `isNonFastForwardRejection` classifier; used by GitManager's own working-tree git calls AND, since push-rejection classification shipped, by `worker/cms-worker.ts`'s `pushBranchToGitHub` and `pushSettingsBranches` (previously test-only) |
| branch-registry.ts       | Branch tracking (BranchRegistry class, cache-based listing); `scanBranchDirectories()` quarantines (skips + `console.warn`s) any dir whose `BranchMetadataFileManager.loadOnly()` throws (e.g. `BranchMetadataCorruptError`) instead of failing the whole listing — the broken dir stays on disk for `branch-health.ts`/admin recovery; generation-token snapshot cache (resource-generation.ts, no more `branches.stale.json`) — eager regen on `invalidate()`, `get()`-miss backstop; see docs/concurrency.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| branch-health.ts         | Admin observability/recovery scan (PR-A3): `scanBranchHealth(baseRoot, { baseBranchName })` classifies every dir under a branches root as `healthy`/`corrupt-metadata`/`orphan`, mirroring branch-registry.ts's own skip rules so classifications never disagree with what the registry shows as "missing"; surfaces the provisioning-lock's freshness (`{baseRoot}/.{dirName}.init.lock`, same path as `branch-workspace.ts`'s `ensureGitWorkspace()`) for orphan/corrupt entries; consumed by `api/admin-branch-health.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| branch-workspace.ts      | Workspace management (BranchWorkspaceManager class); `openOrCreateBranch` resolves the fork point via `resolveBaseBranch` (utils/git.ts) and records it as `baseBranch` in branch metadata; `ensureGitWorkspace` (private) requires a `baseBranch` argument                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| branch-metadata.ts       | Branch metadata with layered concurrency safety (in-process `withLock` + server-enforced `withOccFileLock` + OCC via `utils/occ-json-write.ts`, retry-on-conflict); `save()` treats `baseBranch` as immutable after creation (existing value always wins); `buildMergedBranchUpdate(branchName, now?)` — shared archived-branch delta (status 'archived', pullRequestState 'merged', mergedAt) used by both `CmsWorker.pollMergeState` and the manual `markAsMerged` API so both paths produce identical archived-branch metadata; `BranchMetadataCorruptError` — thrown when `branch.json` exists but fails to parse (distinguished from provisioning/IO failures so callers degrade instead of hard-failing: `branch-registry.ts`'s scan quarantines the branch, and the base-branch request path keeps serving with empty internal groups); see docs/concurrency.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| branch-schema-cache.ts   | Per-branch schema caching with invalidation (always file-based); generation-token cache (resource-generation.ts, no more `.stale` file), exports `SCHEMA_GENERATION_RESOURCE`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| settings-workspace.ts    | Settings branch workspace management                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| settings-branch-utils.ts | Settings branch utility helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| github-service.ts        | GitHub API integration (PR creation, etc.); `createOrUpdatePullRequest()` (shared idempotent helper, also used by CmsWorker), `createCanopyOctokit()` (throttling-plugin factory), `shouldRetryRateLimit`/`shouldRetrySecondaryRateLimit` — see Worker Module                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### Key Types

- BranchContext - Branch state with paths (branchRoot, baseRoot) and metadata
- BranchMetadata - Branch info (name, status, access, timestamps, baseBranch — fork point recorded at creation, immutable afterwards (enforced in branch-metadata.ts save()), conflictStatus, conflictFiles, pullRequestState — `PullRequestState` last observed by the worker's merge-poll or `markAsMerged`, mergedAt — ISO timestamp stamped when archived via PR merge, rebaseFailure — `{ message, firstAt, lastAt }` set by the worker when a rebase cycle fails for this branch, cleared on next success/submit, syncFailureReason — short reason the last GitHub sync task failed, set by the worker alongside `syncStatus: 'sync-failed'`, cleared on next successful sync)
- BranchPaths - Path information (baseRoot, branchRoot)
- SyncStatus - 'synced' | 'pending-sync' | 'sync-failed' (for async task queue)

### Protected Base Branch

The base branch (`config.defaultBaseBranch`, usually `main`) can never be submitted for review in either mode (submitting it would push straight to itself, bypassing review) and is read-only in the editor in prod only (dev needs it editable since the developer always lands there). See `authorization/protected-branch.ts` (`getBranchProtection`) for the single source of truth other modules key off of.

| Layer               | Enforcement                                                                                                                                                                                                                                                     |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API guards          | `writableBranch`/`submittableBranch` (api/guards.ts) — 403 before the handler runs; see API Layer above                                                                                                                                                         |
| Workflow permission | `authorization/branch.ts`'s `canPerformWorkflowAction(..., { isProtectedBranch })` disables the auto-provisioned system-branch (`createdBy: 'canopycms-system'`) grant on the base branch                                                                       |
| Branch delete       | `api/branch.ts`'s `deleteBranchHandler` rejects (400) deleting the protected base branch — it's the prod serving clone                                                                                                                                          |
| Branch listing      | `BranchListItem` (api/branch.ts) carries `isProtected`/`readOnly`, computed server-side per request                                                                                                                                                             |
| Editor UI           | `BranchManager.tsx` hides Submit and shows a "protected" badge/tooltip; `EditorHeader.tsx` disables Save/Submit and shows a `protected-branch-banner` Alert when `branchReadOnly`; both driven by `currentBranch.isProtected`/`.readOnly` from the branches API |

### Operating Mode Module

**Location**: packages/canopycms/src/operating-mode/

| File                      | Purpose                                                                                                                                                                                                                    |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| index.ts                  | Public API; exports `OperatingMode = 'prod' \| 'dev'` and factory functions; re-exports `resolveDeploymentName`                                                                                                            |
| client-safe-strategy.ts   | `ProdClientSafeStrategy`, `DevClientSafeStrategy` (no Node.js imports)                                                                                                                                                     |
| client-unsafe-strategy.ts | `ProdStrategy`, `DevStrategy` — full server-side strategies; `getSettingsBranchName` on both now delegates to `resolveDeploymentName` (deployment-name.ts) instead of inlining `config.deploymentName ?? 'prod'`/`'local'` |
| deployment-name.ts        | `resolveDeploymentName(config, modeDefault)` — single resolution point for `deploymentName`, which namespaces the settings branch (`canopycms-settings-{deploymentName}`); see "Settings Branch Namespacing" below         |
| types.ts                  | `ClientSafeStrategy`, `ClientUnsafeStrategy`, `RemoteUrlConfig` interfaces                                                                                                                                                 |

**Operating Modes** (`OperatingMode = 'prod' | 'dev'`):

- `prod`: Branch clones on persistent filesystem (e.g., EFS at `/mnt/efs/workspace`). Full PR workflow, real GitHub integration, task-queue-based git ops. Env var: `CANOPYCMS_WORKSPACE_ROOT`.
- `dev`: Branch clones in `.canopy-dev/` (gitignored). Full-featured branching and git ops (same as prod), but no real GitHub PRs (`supportsPullRequests()` → false). Auto-detects current git HEAD branch as `defaultActiveBranch` and `defaultBaseBranch` (fork point) when not explicitly configured.

**Strategy classes**:

| Class                  | Layer       | Mode | Key behaviour                                               |
| ---------------------- | ----------- | ---- | ----------------------------------------------------------- |
| ProdClientSafeStrategy | client-safe | prod | All features true; commits + pushes enabled                 |
| ProdStrategy           | server      | prod | Workspace root from env/EFS; auto-detects remote.git        |
| DevClientSafeStrategy  | client-safe | dev  | All features true except `supportsPullRequests→false`       |
| DevStrategy            | server      | dev  | Workspace root: `{cwd}/.canopy-dev`; auto-init local remote |

**Usage**:

```typescript
// Client components (safe for 'use client')
import { clientOperatingStrategy } from '@/operating-mode'
const strategy = clientOperatingStrategy(mode)
if (strategy.supportsBranching()) { ... }

// Server code
import { operatingStrategy } from '@/operating-mode'
const strategy = operatingStrategy(mode)
const branchRoot = strategy.getContentBranchRoot('my-branch')
```

**Settings Branch Namespacing (`deploymentName`)**

Two CanopyCMS deployments (e.g. staging + prod, or two separate apps) can share one GitHub repo without fighting over the same settings orphan branch (`canopycms-settings-{deploymentName}`, which holds `permissions.json`/`groups.json`). `getSettingsBranchName()` on both `ProdStrategy`/`DevStrategy` resolves the name via `resolveDeploymentName(config, modeDefault)` (operating-mode/deployment-name.ts), the single resolution point, in this precedence order:

| Precedence | Source                                                | Why                                                                                                                                                                          |
| ---------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 (wins)   | `process.env.CANOPYCMS_DEPLOYMENT_NAME`               | Trimmed, empty ignored; stamped per-stack by infra (canopycms-cdk's `CanopyCmsServiceProps.deploymentName`), so it's GUARANTEED TO DIFFER between deployments sharing a repo |
| 2          | `config.deploymentName`                               | Baked into the shared repo's `canopycms.config.ts`, so it's GUARANTEED TO BE IDENTICAL across deployments sharing it                                                         |
| 3          | `modeDefault` (caller-supplied, `'prod'` / `'local'`) | Mode-specific fallback when neither of the above is set                                                                                                                      |

Warns once (module-level flag) if env and config are both set and disagree, naming which one won. See `settings-workspace.ts`'s `ensureGitWorkspace` rename guard for the failure this protects against: without it, an existing settings workspace resolving to a newly-different branch name would be silently wiped (`git checkout --orphan` + `rm -rf .`) instead of refusing to boot.

### Branch Lifecycle

1. Create branch -> BranchWorkspaceManager provisions clone
2. Edit content -> Writes to branch workspace
3. Submit for merge -> Commits, pushes, creates PR via Octokit (or queues task for worker)
4. Review -> Request changes unlocks, approval locks
5. Merge -> Clean up remote branch, archive clone (via manual `markAsMerged` API, or automatically by CmsWorker's `pollMergeState` detecting the PR merged on GitHub — both share `buildMergedBranchUpdate`, see branch-metadata.ts)

### Storage

- .canopycms/branch.json - Per-branch metadata
- .canopycms/branches.json - Branch registry
- .canopycms/comments.json - Comment threads

### GitHub Sync (Direct vs. Async)

**Location**: packages/canopycms/src/api/github-sync.ts

In prod mode, Lambda has no internet access. GitHub operations are handled two ways:

| Scenario                | Path       | Description                                                          |
| ----------------------- | ---------- | -------------------------------------------------------------------- |
| githubService available | Direct     | Calls GitHub API immediately (dev mode, or if internet is available) |
| No githubService        | Task queue | Enqueues task for EC2 worker; branch gets syncStatus 'pending-sync'  |

The `syncSubmitPr()` and `syncConvertToDraft()` functions in github-sync.ts abstract this choice.

### Git Operations (Service Methods)

**Location**: packages/canopycms/src/services.ts

Service methods handle git operations with automatic author handling:

| Method                     | Purpose                     | Usage                                                  |
| -------------------------- | --------------------------- | ------------------------------------------------------ |
| `commitFiles()`            | Commit specific files       | Admin changes (permissions, groups)                    |
| `submitBranch()`           | Full submit workflow        | Branch submission (checkout, status, commit all, push) |
| `commitToSettingsBranch()` | Commit to settings branch   | Permission/group changes with optional PR              |
| `getSettingsBranchRoot()`  | Get settings workspace root | Ensures workspace exists                               |

**Pattern**: Use `context.branchRoot` directly instead of `resolveBranchPaths()`. BranchContext extends BranchPaths, so it already has `branchRoot` and `baseRoot` properties.

**commitFiles Example** (permissions.ts, groups.ts):

```typescript
await ctx.services.commitFiles({
  context,
  files: '.canopycms/permissions.json',
  message: 'Update permissions',
})
```

**submitBranch Example** (branch-status.ts):

```typescript
await ctx.services.submitBranch({ context })
```

**Git Author Handling**: Both methods automatically call `git.ensureAuthor()` using `gitBotAuthorName` and `gitBotAuthorEmail` from config. No manual author setup needed.

**GitManager.add()**: Accepts `string | string[]` for convenience.

**Branch identity** (`services.ts` → `createActiveBranchDetector()`): Service creation resolves and bakes BOTH branch-identity fields into config so all downstream code reads one consistent value. `defaultActiveBranch` (which workspace to serve content from): explicit config wins in both modes; `dev` auto-detects from git HEAD (`git rev-parse --abbrev-ref HEAD`, 5s TTL cache); `prod` falls back to `defaultBaseBranch ?? 'main'`. `defaultBaseBranch` (fork point for new editing branches): explicit config wins; `dev` follows the same HEAD detection when unset; otherwise `'main'` (matching `resolveBaseBranch` in utils/git.ts, the canonical definition used by workspace provisioning). `content-reader.ts` uses `defaultActiveBranch` as its default branch. `services.refreshActiveBranch()` re-detects git HEAD in dev mode and refreshes both fields (each only when not explicitly configured); it short-circuits when mode !== 'dev', when both fields are explicitly configured, or when `deployedAs: 'static'`. Called by `getContext()` and the HTTP handler before each request. `createTestCanopyServices` pins both fields (`defaultBaseBranch ?? 'main'`, `defaultActiveBranch ?? defaultBaseBranch ?? 'main'`) to avoid HEAD detection in tests. Git-operating service methods (`commitFiles`, `submitBranch`) prefer the branch's recorded `context.branch.baseBranch` over the config value.

## Path Utilities Module

**Location**: packages/canopycms/src/paths/

| File                | Purpose                                                                                                                                                                                                                                                                                                                          |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| types.ts            | Branded types: LogicalPath, PhysicalPath, BranchName, SanitizedBranchName, ContentId, Slug; `ROOT_COLLECTION_ID` sentinel                                                                                                                                                                                                        |
| normalize.ts        | Client-safe normalization; createLogicalPath, createPhysicalPath, unsafeAsLogicalPath, unsafeAsPhysicalPath                                                                                                                                                                                                                      |
| normalize-server.ts | Server-only functions (requires Node.js path)                                                                                                                                                                                                                                                                                    |
| validation.ts       | Security validation; parseLogicalPath, parsePhysicalPath, parseBranchName, parseContentId, parseSlug                                                                                                                                                                                                                             |
| resolve.ts          | resolveLogicalPath for path resolution                                                                                                                                                                                                                                                                                           |
| branch.ts           | Branch workspace path RESOLUTION (resolveBranchPath, ensureBranchRoot, resolveBranchPaths); imports `node:fs`/`node:path` and operating-mode strategies — server-only, not client-bundle-safe. Re-exports `sanitizeBranchName` from branch-name.ts for existing server-side importers.                                           |
| branch-name.ts      | Dependency-free home of `sanitizeBranchName` (types-only import). Split out of branch.ts so client-reachable code (e.g. authorization/protected-branch.ts, which the editor bundle reaches via api/guards.ts) can sanitize branch names without pulling in branch.ts's node built-ins. Same pattern as assets/asset-prefixes.ts. |
| test-utils.ts       | Test-only casts: unsafeAsBranchName, unsafeAsSlug (NOT exported from index)                                                                                                                                                                                                                                                      |

**Client-bundle boundary**: `sanitizeBranchName` must always be imported from `paths/branch-name` in client-reachable code, never from `paths/branch` or the `paths` barrel (`paths/index.ts` re-exports `branch.ts`'s node-dependent exports alongside it, so importing anything from the barrel pulls `branch.ts`'s `node:fs` into the dependency graph). Getting this wrong has caused two production `next build` regressions of the editor bundle; `pnpm lint:bundle` (see DEVELOPING.md) guards against regressions.

**Branded Types** provide type safety for different path semantics:

- `LogicalPath`: Content-relative paths without embedded IDs (e.g., "content/posts/my-post"); used for all collection paths
- `PhysicalPath`: Filesystem paths that may contain embedded content IDs
- `BranchName`: Git branch names (validated against git naming rules)
- `SanitizedBranchName`: Branch name safe for filesystem use
- `ContentId`: 12-character Base58-encoded content ID
- `Slug`: Single path segment for collections or entries (e.g., "posts", "my-first-post"); normalized to lowercase
- `ROOT_COLLECTION_ID`: Sentinel `ContentId` value (`'__rootcoll__'`) for the root content directory, which has no embedded ID in its name. Uses underscores to avoid collision with real Base58 IDs.

Note: `CollectionPath` brand was eliminated; use `LogicalPath` with a `content/` prefix for all collection paths.

### Branded Type Conventions

**Boundary validation** (`parse*` functions) - validate before branding, use at API/input boundaries:

```typescript
import { parseLogicalPath, parseBranchName, parseContentId, parseSlug } from '../paths'

const result = parseLogicalPath(params.path)
if (!result.ok) return { status: 400, error: result.error }
const path: LogicalPath = result.path
```

**Trusted internal construction** (`create*` functions) - validate segments, throw on traversal:

```typescript
import { createLogicalPath, createPhysicalPath } from '../paths'

const path = createLogicalPath('content', 'posts', 'my-post')
```

**Unsafe production casts** (`unsafeAs*` in normalize.ts/validation.ts) - NO validation, for trusted internal data only:

```typescript
import { unsafeAsLogicalPath, unsafeAsPhysicalPath } from '../paths'
import { unsafeAsSlug } from '../paths/test-utils'

// OK (test-only): data already validated on write, read from internal storage
const logicalPath = unsafeAsLogicalPath(entry.collection)
```

**Test-only casts** (in test-utils.ts, NOT exported from index):

```typescript
import { unsafeAsBranchName, unsafeAsSlug } from '../paths/test-utils'
```

### Zod Validators for API Boundaries

**Location**: packages/canopycms/src/api/validators.ts

| Schema               | Branded Type   | Validates                               |
| -------------------- | -------------- | --------------------------------------- |
| branchNameSchema     | BranchName     | Git naming rules                        |
| logicalPathSchema    | LogicalPath    | No traversal, not a physical path       |
| contentIdSchema      | ContentId      | 12-char Base58                          |
| slugSchema           | Slug           | Lowercase, hyphens, max 64 chars        |
| permissionPathSchema | PermissionPath | No traversal, from authorization module |

```typescript
import { branchNameSchema, logicalPathSchema } from '../api/validators'

const paramsSchema = z.object({
  branch: branchNameSchema,
  path: logicalPathSchema,
})
const params = paramsSchema.parse(req.params)
// params.branch is BranchName, params.path is LogicalPath
```

### PermissionPath (authorization module)

`PermissionPath` is a branded type defined in `authorization/types.ts`, not in `paths/`.

- Production: use `permissionPathSchema` from `api/validators.ts` or `parsePermissionPath` from `authorization`
- Tests: use `unsafeAsPermissionPath` from `authorization/test-utils.ts`

## Validation Module

**Location**: packages/canopycms/src/validation/

| File                    | Purpose                                                                                                                                                                         |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| field-traversal.ts      | Schema-aware field traversal                                                                                                                                                    |
| reference-validator.ts  | Reference field validation (ID format, existence, collection constraints, entry type constraints via extractEntryTypeFromFilename)                                              |
| entry-link-validator.ts | Validates `entry:ID` patterns in body/markdown fields at save time; produces warnings (never blocks saves) for broken links; exports `validateEntryLinks()`, `EntryLinkWarning` |
| deletion-checker.ts     | Referential integrity checking; `findIdInData` recurses through inline groups                                                                                                   |

**Field Traversal** - generic way to walk nested data according to schema:

```typescript
import { traverseFields, findFieldsByType } from '../validation/field-traversal'

// Find all reference fields in data
const refs = findFieldsByType(schema, data, 'reference')
```

## Entry Links

Cross-cutting feature for linking between content entries using `entry:CONTENT_ID` syntax in markdown body content. Extends the reference-by-ID pattern (used for structured reference fields) to inline links.

**Syntax**: `[Link text](entry:vh2WdhwAFiSL)` or `[Link text](entry:vh2WdhwAFiSL#section-heading)`

| Layer      | File                                              | Purpose                                                                                               |
| ---------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Core       | src/entry-link-resolver.ts                        | `resolveEntryUrl()`, `resolveEntryLinksInText()`, `extractEntryLinkIds()`; code-block-safe resolution |
| Validation | src/validation/entry-link-validator.ts            | `validateEntryLinks()` — warnings for broken links at save time (never blocks saves)                  |
| Config     | src/config/types.ts                               | `entryLinkUrl?: EntryLinkUrlResolver` callback on `CanopyConfig` for custom URL mapping               |
| Read path  | src/content-reader.ts                             | Resolves entry links in body at read time (default on; opt-out via `resolveEntryLinks: false`)        |
| Write path | src/api/content.ts                                | Validates entry links on write; returns `entryLinkWarnings` in response                               |
| AI         | src/ai/generate.ts                                | Resolves entry links in body before AI content generation                                             |
| Preview    | src/editor/hooks/useEntryLinkResolution.ts        | Client-side hook: builds URL map from editor entries, resolves patterns in preview data               |
| Toolbar    | src/editor/fields/entry-link/InsertEntryLink.tsx  | MDXEditor toolbar button + entry picker modal                                                         |
| Context    | src/editor/fields/entry-link/EntryLinkContext.tsx | React context bridging editor entry list to toolbar components                                        |

**Server exports** (`canopycms/server`): `resolveEntryUrl`, `resolveEntryLinksInText`, `extractEntryLinkIds`, `EntryLinkUrlResolver`

**Client exports** (`canopycms/client`): `EntryLinkContext`, `useEntryLinkContext`, `EntryLinkOption`, `EntryLinkContextValue`

**Resolution behavior**: Missing IDs resolve to `#` (dead link) with a warning logged. Anchor fragments are preserved. Code blocks (fenced and inline) are skipped to avoid corrupting code examples.

## Utility Module

**Location**: packages/canopycms/src/utils/

**Subpath export**: `canopycms/utils/error` — `error.ts` is exposed as a package subpath (in both dev exports and `publishConfig.exports`) for adopter/satellite use.

| File                    | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| error.ts                | Type-safe error handling (getErrorMessage, isNodeError, isNotFoundError, isFileExistsError); also exported via `canopycms/utils/error`. `sanitizeErrorMessage(message)` redacts known-sensitive shapes from client-facing error messages (git stderr is unbounded, so shapes are redacted rather than messages enumerated): URL-embedded credentials → `***@`, paths under process.cwd() → relative, absolute paths outside cwd → `<path>`, Windows drive paths → `<path>`; log the original server-side and send the sanitized form to clients (used by http/handler.ts 503 path). `redactCredentials(message)` redacts only credential material (URL userinfo like `https://token@host`, bare GitHub token shapes `ghp_…`/`github_pat_…`, `Bearer <token>` values) while keeping filesystem paths intact — for server-side log lines that deliberately keep full path detail but must never persist a live token (used by api/branch-status.ts and http/handler.ts console.error calls); `sanitizeErrorMessage` calls it internally, then also redacts paths  |
| debug.ts                | Debug logging utilities (createDebugLogger)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| format.ts               | Content format utilities (getFormatExtension, isDataOnlyFormat); supports md/mdx/json/yaml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| atomic-write.ts         | Atomic file writes via temp-file + rename (prevents corruption on NFS/EFS)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| body-field.ts           | Body field validation (countBodyFields, findInvalidBodyFields) for isBody schema flag; all three functions use `flattenGroupFields` to traverse inline groups transparently                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| title-field.ts          | Title field utilities (resolveEntryTitle, findInvalidTitleFields, findTitleFieldsInLists) for isTitle schema flag                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| flatten-group-fields.ts | `flattenGroupFields(fields)` — recursively flattens inline `type: 'group'` fields into a flat `FieldConfig[]` for data-layer iteration; used by body-field.ts, json-to-markdown.ts, useReferenceResolution, and client-reference-resolver; not needed when using `traverseFields()` (which already handles groups)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| git.ts                  | `detectHeadBranch()` — detect current HEAD branch name via simple-git (used by services.ts and ai/resolve-branch.ts); `resolveBaseBranch({ defaultBaseBranch?, mode, detectFrom? })` — canonical resolver for the base branch (fork point): explicit config wins, dev mode detects git HEAD, otherwise `'main'`; used by GitManager.initializeWorkspace and BranchWorkspaceManager.openOrCreateBranch; `isNonFastForwardRejection(message)` — classifies a git push-failure message as git's non-fast-forward rejection (`[rejected]` + `non-fast-forward`/`fetch first`, or the "Updates were rejected because" hint) — the signature of two CanopyCMS deployments sharing one GitHub repo colliding on the same content-branch name; deliberately narrow (ordinary transient failures — network/auth/lock — stay retryable); requires stable English git output, hence `gitChildEnv`'s forced `LC_ALL=C`/`LANG=C` (git-manager.ts); used by `api/branch-status.ts` (409 response) and `worker/cms-worker.ts` (`pushBranchToGitHub` fail-fast)                 |
| fs.ts                   | Filesystem helpers (filePathExists)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| sanitize-href.ts        | Sanitize untrusted URLs for href attributes; allows only http/https protocols (sanitizeHref)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| occ-json-write.ts       | Shared OCC (optimistic-concurrency-control) JSON write helper, extracted from near-identical logic previously duplicated in comment-store.ts and branch-metadata.ts (both now adopt it, alongside authorization/settings-file-store.ts's `mutateSettingsJsonFile` for permissions.json/groups.json): `OccWriteConflictError`; `writeOccJsonFile(filePath, payload, { expectedVersion, settleMs?, trailingNewline? })` — temp-file write, version precheck, atomic rename (or temp+`link()` exclusive-create for new files), settle wait, writeId read-back verification; `withOccRetry(operation, { maxAttempts?, isRetryable? })` — retries on `OccWriteConflictError` with exponential backoff + jitter, caller reloads state each attempt; `withOccFileLock(filePath, fn)` — server-enforced mutual exclusion via `proper-lockfile` (mkdir-based, immune to client-side NFS caching), for writers where a cross-host lost update is unacceptable. See docs/concurrency.md for the guarantee/non-guarantee boundary between OCC alone and the lockfile layer. |
| provisioning-lock.ts    | Cross-process filesystem lock (via `proper-lockfile`) for content provisioning, so parallel build-worker processes (or admin requests) don't race creating/mutating the same git workspace. `acquireProvisioningLock(lockTargetDir, lockName)` — patient variant, ~600 jittered retries (minutes) for build workers that can afford to wait on a live provisioner. `tryAcquireProvisioningLock(lockTargetDir, lockName)` — zero-retry sibling for a synchronous request/response cycle (e.g. Lambda-backed admin handlers); throws immediately with `err.code === 'ELOCKED'` on live contention instead of retrying, so callers can translate that into a 409. Both share `stale: 30_000` staleness recovery; used by branch-workspace.ts's `ensureGitWorkspace()` and by `api/admin-branch-health.ts`'s purge handler (which also reads the same lock's mtime via branch-health.ts's `provisioningLockPath` to gate on freshness)                                                                                                                              |

**Error Handling Pattern**:

```typescript
import { getErrorMessage, isNotFoundError } from '../utils/error'

try {
  await fs.readFile(path)
} catch (err: unknown) {
  if (isNotFoundError(err)) return null
  throw new Error(`Failed: ${getErrorMessage(err)}`)
}
```

**Atomic Write Pattern** (used by content-store.ts, schema-store.ts): permissions.json/groups.json (authorization/permissions/loader.ts, authorization/groups/loader.ts) no longer call `atomicWriteFile` directly — they write through `mutateSettingsJsonFile` (authorization/settings-file-store.ts), whose OCC layer (`writeOccJsonFile`, occ-json-write.ts) does its own temp-file+rename write under the cross-host lock.

```typescript
import { atomicWriteFile } from '../utils/atomic-write'

await atomicWriteFile(filePath, JSON.stringify(data, null, 2) + '\n')
```

## AI Module

**Location**: packages/canopycms/src/ai/

Lightweight, read-only AI content serving. Does not require auth or the editor API.

| File                    | Purpose                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| handler.ts              | `createAIContentHandler()` — Next.js GET handler for AI-ready content                                                                                                                                                                                                                                                                                                    |
| generate.ts             | `generateAIContent()` — converts entries to AI-ready markdown; resolves `entry:ID` links in body before output; `runEntryTransform()` runs the per-entry `entryTransforms` hook once and caches output on `AIEntry.appendedSections`; `makeReadSibling()` builds the traversal-guarded `ctx.readSibling` (rejects slashes/`..`/absolute paths, resolves null if missing) |
| json-to-markdown.ts     | Schema-driven entry-to-markdown converter; body pipeline: `stripMdxImports → componentTransforms → bodyTransforms`; outer field loop uses `flattenGroupFields` (group early-return removed); appends `entry.appendedSections` (entry-transform output) after body/fields                                                                                                 |
| transform-components.ts | `applyComponentTransforms(body, transforms)`, `parseComponentProps(attrString)` — parses JSX from MDX and applies adopter-defined transforms to clean markdown; regex + convergence loop, code-block and inline-code aware                                                                                                                                               |
| resolve-branch.ts       | `resolveBranchRoot()` — resolves branch root for AI handler; mirrors `createActiveBranchDetector` priority (explicit config > git HEAD in dev > fallback)                                                                                                                                                                                                                |
| strip-mdx.ts            | `stripMdxImports()` — removes import/export statements from MDX body content for clean AI consumption                                                                                                                                                                                                                                                                    |
| types.ts                | `AIContentConfig`, `AIEntryMeta`, `ExcludeConfig`, `BundleConfig`, `FieldTransforms`, `ComponentProps`, `ComponentTransformFn`, `ComponentTransforms`, `BodyTransformFn`, `BodyTransforms`, `EntryTransformFn`, `EntryTransforms`, `EntryTransformContext`, manifest types                                                                                               |

**Transform hooks on `AIContentConfig`** (all keyed by entry type):

- `fieldTransforms` / `componentTransforms` / `bodyTransforms` — field-, JSX-component-, and body-level rewrites (body transforms fire for MD/MDX only).
- `entryTransforms?: EntryTransforms` (`Record<string, EntryTransformFn>`) — entry-level hook that runs once per entry at generation time and appends returned markdown after the body/fields. Fires for every format (including data-only JSON/YAML), unlike `bodyTransforms`. Output flows automatically into the per-entry file, the collection `all.md`, and bundles.
- `EntryTransformFn = (entry: AIEntry, ctx: EntryTransformContext) => Promise<string | undefined> | string | undefined`; `EntryTransformContext = { contentId; readSibling(name) }`. `ctx.readSibling` is traversal-guarded (rejects slashes/`..`/absolute paths, resolves null if the file is missing), letting an adopter fold a colocated sibling artifact (e.g. `<contentId>.profile.json`) into AI output without exposing the entry's absolute path. A throwing transform is logged and skipped; the entry still renders without the appended section.

**Caching strategy in `createAIContentHandler()`**:

- `prod`: result cached for the process lifetime (Lambda instances recycled on deploy, so fresh on each deploy)
- `dev` (`mode !== 'prod'`): cache cleared on every request so content changes are reflected immediately without restarts

**Schema cache**: Uses `BranchSchemaCache` (file-based); no special in-memory path.

**Cache-Control headers**: `no-cache` when `mode !== 'prod'`; `public, max-age=60` in prod.

**Static file generation** (`src/build/generate-ai-content.ts`, `generateAIContentFiles()`): writes AI content to disk (e.g. `public/ai`) for the `canopycms generate-ai-content` CLI command. Unconditionally validates every entry via `assertBuildEntriesValid` (`static/index.ts`) before generating — no `isBuildMode()` gate, since this is an explicit build command rather than something `next dev` runs incidentally.

## HTTP Module

**Location**: packages/canopycms/src/http/

| File       | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| types.ts   | CanopyRequest, CanopyResponse types                                                                                                                                                                                                                                                                                                                                                                                                                           |
| router.ts  | Route matching and dispatch                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| handler.ts | Request handler factory; rejects anonymous callers (401) immediately after authentication, BEFORE base-branch workspace provisioning, so they can neither trigger provisioning nor read provisioning errors; first-request provisioning failure returns 503 with the error message sanitized via `sanitizeErrorMessage` (server logs keep full path detail via console.error, credential-redacted with `redactCredentials`) instead of an unhandled rejection |

## Test Utilities

**Location**: packages/canopycms/src/test-utils/

**Subpath export**: `canopycms/test-utils` — available to satellite packages (e.g., canopycms-auth-clerk) and adopter test suites.

| File                | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| api-test-helpers.ts | Mock factories: createMockBranchContext, createMockUser, createMockServices, createMockApiContext, etc.; `createMockSettingsMutation({ currentFile? })` — mocks `mutatePermissionsFile`/`mutateGroupsFile`'s `(root, mode, mutate) => ...` shape, returning `{ impl, getPayload() }` so a test can `vi.mocked(...).mockImplementation(settingsMutation.impl)` and assert on the payload the handler's `mutate` callback produced |
| console-spy.ts      | mockConsole() + consoleMatchers (toHaveLogged, toHaveWarned, toHaveErrored)                                                                                                                                                                                                                                                                                                                                                      |
| git-helpers.ts      | initTestRepo() — initialize a git repo with CanopyCMS marker and user config                                                                                                                                                                                                                                                                                                                                                     |

**Integration Tests**: packages/canopycms/src/\_\_integration\_\_/

## Example App

**Location**: apps/example1/

### Adopter Touchpoints (Keep Minimal!)

1. canopycms.config.ts - Schema definition
2. app/api/canopycms/[...canopycms]/route.ts - Catch-all API handler
3. app/edit/page.tsx - Editor component embedding
4. app/lib/canopy.ts - Canopy context setup (exports `getCanopy`, phase-selecting `readByUrlPath`/`read`, `contentStaticParams`, `getHandler`; `getCanopyForBuild` as advanced escape hatch)
5. app/schemas.ts - Entry schema definitions
6. app/ai/config.ts - AI content configuration
7. app/ai/[...path]/route.ts - AI content endpoint
8. middleware.ts - Auth route protection
9. next.config.mjs - Uses `withCanopy` from `canopycms-next/config`

### Expected Structure

```
apps/example1/
  app/
    ai/config.ts                            # AI content config
    ai/[...path]/route.ts                   # AI content endpoint
    api/canopycms/[...canopycms]/route.ts   # Catch-all API
    edit/page.tsx                            # Editor page
    lib/canopy.ts                           # Context setup
    schemas.ts                              # Entry schemas
    layout.tsx
  content/                                  # Content files
  canopycms.config.ts                      # Config
  middleware.ts                             # Auth protection
  next.config.mjs                          # Next.js config (withCanopy)
```

**Dev scripts**: `pnpm reset-sim` removes `.canopy-dev/` (local dev workspaces + simulated remote).

## Test Organization

- **Unit tests**: Co-located in `__tests__/` subdirectories within each module
- **Integration tests**: `packages/canopycms/src/__integration__/`
- **Test utilities**: `packages/canopycms/src/test-utils/`

## Key Directories to Monitor

When maintaining this guide, watch for changes in:

```
packages/canopycms/src/api/          # API endpoints
packages/canopycms/src/authorization/# Auth & permissions
packages/canopycms/src/auth/         # Auth plugins and cache
packages/canopycms/src/config/       # Configuration
packages/canopycms/src/schema/       # Schema loading and CRUD
packages/canopycms/src/paths/        # Path utilities
packages/canopycms/src/operating-mode/ # Mode strategies
packages/canopycms/src/editor/       # UI components
packages/canopycms/src/assets/       # Asset store, finalize pipeline, transform engine
packages/canopycms/src/validation/   # Validation utilities
packages/canopycms/src/static/       # Framework-agnostic static-generation helpers
packages/canopycms/src/utils/        # Shared utilities
packages/canopycms/src/worker/       # CMS Worker daemon
packages/canopycms/src/task-queue/   # Generic task queue
packages/canopycms/src/cli/          # CLI bootstrapping
packages/canopycms-cdk/              # AWS CDK constructs
packages/canopycms-next/             # Next.js adapter package
packages/canopycms-auth-dev/         # Dev auth plugin + cache
apps/example1/                       # Example app
```
